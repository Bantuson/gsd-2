---
phase: 11.1-pre-v2.0-stabilization
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mission-control/tests/pipeline-config-bridge.test.ts
  - packages/mission-control/tests/ws-server-bind.test.ts
  - packages/mission-control/tests/server-cors.test.ts
  - packages/mission-control/tests/pipeline-wire-guard.test.ts
  - packages/mission-control/tests/error-boundary.test.tsx
autonomous: true
requirements:
  - SC-1
  - SC-2
  - SC-3
  - SC-4
  - SC-6

must_haves:
  truths:
    - "Five test files exist in packages/mission-control/tests/ covering SC-1 through SC-6"
    - "Each test file has at least one failing test (RED state) that describes expected behavior"
    - "Running bun test on these files produces test failures, not syntax or import errors"
  artifacts:
    - path: "packages/mission-control/tests/pipeline-config-bridge.test.ts"
      provides: "Failing tests for SC-1: config values flow from config.json to SessionManager and processFactory"
    - path: "packages/mission-control/tests/ws-server-bind.test.ts"
      provides: "Failing test for SC-3: WebSocket server binds to 127.0.0.1"
    - path: "packages/mission-control/tests/server-cors.test.ts"
      provides: "Failing test for SC-3: CORS header is http://localhost:4000 not *"
    - path: "packages/mission-control/tests/pipeline-wire-guard.test.ts"
      provides: "Failing test for SC-4: wireSessionEvents called twice does not duplicate events"
    - path: "packages/mission-control/tests/error-boundary.test.tsx"
      provides: "Failing test for SC-6: ErrorBoundary renders fallback when child throws"
  key_links:
    - from: "tests/pipeline-config-bridge.test.ts"
      to: "src/server/pipeline.ts"
      via: "import of startPipeline or createPipelineFactory"
      pattern: "from.*pipeline"
    - from: "tests/ws-server-bind.test.ts"
      to: "src/server/ws-server.ts"
      via: "import of createWsServer"
      pattern: "from.*ws-server"
    - from: "tests/error-boundary.test.tsx"
      to: "src/components/ErrorBoundary.tsx"
      via: "import of ErrorBoundary (file does not exist yet — test will fail at import)"
      pattern: "from.*ErrorBoundary"
---

<objective>
Create five failing test stubs that define the expected behaviors for SC-1, SC-3, SC-4, and SC-6
before any implementation code is written (Nyquist Wave 0 requirement).

Purpose: Ensure every automated verify command referenced in plans 01–03 has a test file that can
run. Without these stubs, bun test commands in subsequent plans will fail with "no such file"
rather than meaningful assertion failures.

Output: Five test files in packages/mission-control/tests/, each containing at least one
describe/it/test block that will FAIL until the corresponding implementation is complete.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-pre-v2.0-stabilization/11.1-RESEARCH.md
@.planning/phases/11.1-pre-v2.0-stabilization/11.1-VALIDATION.md

<interfaces>
<!-- Existing test conventions from the codebase -->
<!-- Test framework: bun:test — import { describe, it, expect, mock, beforeEach } from "bun:test" -->
<!-- DOM environment: happy-dom — available for .test.tsx files -->
<!-- Test files in packages/mission-control/tests/ -->

From an existing test (permissions-flag.test.ts pattern):
```typescript
import { describe, expect, it } from "bun:test";
// ...
describe("ClaudeProcessManager", () => {
  it("omits --dangerously-skip-permissions when skipPermissions is false", async () => {
    // ...
    expect(args).not.toContain("--dangerously-skip-permissions");
  });
});
```

From packages/mission-control/src/server/ws-server.ts (createWsServer signature):
```typescript
export function createWsServer(port: number, pipeline: PipelineController): BunServer
```

From packages/mission-control/src/server/pipeline.ts (startPipeline signature):
```typescript
export async function startPipeline(planningDir: string, options?: PipelineOptions): Promise<PipelineController>
```

Note: ErrorBoundary does not exist yet — the test import will fail until Plan 03 creates it.
This is intentional RED state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pipeline-config-bridge and ws-server/server-cors test stubs</name>
  <files>
    packages/mission-control/tests/pipeline-config-bridge.test.ts
    packages/mission-control/tests/ws-server-bind.test.ts
    packages/mission-control/tests/server-cors.test.ts
  </files>
  <behavior>
    - pipeline-config-bridge.test.ts:
      - Test: when config.json has `skip_permissions: false`, startPipeline passes skipPermissions:false to processFactory
      - Test: when config.json has `worktree_enabled: true`, startPipeline calls sessionManager.setWorktreeEnabled(true)
      - These tests MUST FAIL until Plan 01 wires the config bridge
    - ws-server-bind.test.ts:
      - Test: createWsServer produces a server whose options include hostname "127.0.0.1"
      - This test MUST FAIL until Plan 01 adds the hostname option
    - server-cors.test.ts:
      - Test: addCorsHeaders (or the server response) sets Access-Control-Allow-Origin to "http://localhost:4000" not "*"
      - This test MUST FAIL until Plan 01 updates the CORS header
  </behavior>
  <action>
    Write three stub test files. Each uses `import { describe, it, expect } from "bun:test"`.

    For pipeline-config-bridge.test.ts: Use a mock processFactory that captures the options passed
    to it. Call a helper that simulates startPipeline with a temp directory containing a config.json
    with `{"skip_permissions": false, "worktree_enabled": true}`. Assert that the captured options
    include `skipPermissions: false` and that setWorktreeEnabled was called with `true`. Since the
    wiring does not yet exist, these assertions will fail — that is correct RED state.

    Import strategy: Import directly from the source modules. If the module shape makes integration
    testing hard at this stage, write the test to import and call the function — the test will fail
    because the behavior is not yet implemented, not because of a bad import.

    For ws-server-bind.test.ts: Import createWsServer from "@/server/ws-server". Create a mock
    pipeline controller. Call createWsServer on an available port. Access the server object's
    hostname property (Bun.serve returns a Server with a `.hostname` field). Assert
    server.hostname === "127.0.0.1". Close the server after. Will FAIL until Plan 01 adds the
    hostname option.

    For server-cors.test.ts: Import the addCorsHeaders helper from "@/server" (or call the server
    route handler directly). Create a sample Response. Pass through addCorsHeaders. Assert that
    response.headers.get("Access-Control-Allow-Origin") === "http://localhost:4000". Will FAIL
    because current value is "*".

    Test naming: Use `describe("SC-1: Config bridge — ...", ...)` to make failures readable.
    Do NOT use --watch or any interactive flags.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test --timeout 5000 tests/pipeline-config-bridge.test.ts tests/ws-server-bind.test.ts tests/server-cors.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Three files exist. bun test runs them without syntax or import errors. Tests fail on
    assertions (RED state), not on missing modules or parse failures.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create pipeline-wire-guard and error-boundary test stubs</name>
  <files>
    packages/mission-control/tests/pipeline-wire-guard.test.ts
    packages/mission-control/tests/error-boundary.test.tsx
  </files>
  <behavior>
    - pipeline-wire-guard.test.ts:
      - Test: calling wireSessionEvents twice on the same session does not register handlers twice
      - Observable: emit one event after double-wiring — handler fires exactly once, not twice
      - MUST FAIL until Plan 02 adds the wired guard
    - error-boundary.test.tsx:
      - Test: when a child component throws during render, ErrorBoundary renders the fallback
      - Observable: rendered output contains fallback text, not the child's text
      - MUST FAIL (import error) until Plan 03 creates ErrorBoundary.tsx
  </behavior>
  <action>
    Write two stub test files.

    For pipeline-wire-guard.test.ts: Import wireSessionEvents from "@/server/pipeline" (or the
    relevant export). Create a minimal mock SessionState with a mock processManager whose onEvent
    accumulates handlers. Call wireSessionEvents twice on the same session. Emit a test event via
    the processManager. Assert the handler fires exactly once (not twice). Will FAIL because no
    wired guard exists yet.

    For error-boundary.test.tsx: Import { ErrorBoundary } from "@/components/ErrorBoundary" (this
    import will fail with "Cannot find module" until Plan 03 creates the file — that IS the
    expected RED state). Use happy-dom + React Testing Library pattern. Create a ThrowingChild
    component that throws during render. Render <ErrorBoundary fallback={<p>Error occurred</p>}>
    <ThrowingChild /></ErrorBoundary>. Assert the output contains "Error occurred".

    For error-boundary.test.tsx, add at the top of the file:
    ```
    // @ts-ignore — ErrorBoundary not yet created; test will fail with import error until Plan 03
    ```
    This prevents the TypeScript error from obscuring the test failure message.

    Use `describe("SC-4: wireSessionEvents double-call guard", ...)` and
    `describe("SC-6: ErrorBoundary fallback on render error", ...)`.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test --timeout 5000 tests/pipeline-wire-guard.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    Both files exist. pipeline-wire-guard.test.ts runs and fails on assertion (RED).
    error-boundary.test.tsx exists with correct structure. The full 5-file stub set is complete.
  </done>
</task>

</tasks>

<verification>
All five Wave 0 stub files exist:
  ls packages/mission-control/tests/pipeline-config-bridge.test.ts
  ls packages/mission-control/tests/ws-server-bind.test.ts
  ls packages/mission-control/tests/server-cors.test.ts
  ls packages/mission-control/tests/pipeline-wire-guard.test.ts
  ls packages/mission-control/tests/error-boundary.test.tsx

Tests run without syntax errors:
  cd packages/mission-control && bun test --timeout 5000 tests/pipeline-wire-guard.test.ts tests/ws-server-bind.test.ts tests/server-cors.test.ts

Existing tests still pass:
  cd packages/mission-control && bun test --timeout 5000 tests/permissions-flag.test.ts tests/session-manager.test.ts
</verification>

<success_criteria>
- Five test stub files created in packages/mission-control/tests/
- Files are syntactically valid TypeScript/TSX
- Tests that can run without the missing ErrorBoundary module produce assertion failures (RED), not parse/import errors
- Existing test suite continues to pass
- Wave 0 complete — plans 01–03 have valid automated verify targets
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-pre-v2.0-stabilization/11.1-00-SUMMARY.md`
</output>
