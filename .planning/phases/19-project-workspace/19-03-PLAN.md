---
phase: 19-project-workspace
plan: 03
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - packages/mission-control/src/components/workspace/ProjectHomeScreen.tsx
  - packages/mission-control/src/components/workspace/ProjectCard.tsx
  - packages/mission-control/src/components/workspace/ProjectCardMenu.tsx
autonomous: true
requirements: [WORKSPACE-02, WORKSPACE-03]

must_haves:
  truths:
    - "With builderMode=false the home screen shows an Open Folder button"
    - "With builderMode=true the home screen shows a brief-taking input (project name field)"
    - "Each project card shows name, relative last-active time, active milestone, progress bar, and a Resume button"
    - "The ··· menu on a card offers Archive, Open in Finder/Explorer, and Remove from list options"
    - "Archived projects are excluded from the main grid; Show archived link reveals them"
  artifacts:
    - path: "packages/mission-control/src/components/workspace/ProjectHomeScreen.tsx"
      provides: "Full-screen project grid — empty state, project cards grid, archived toggle"
      min_lines: 80
    - path: "packages/mission-control/src/components/workspace/ProjectCard.tsx"
      provides: "Project card with progress bar, timestamps, Resume button"
      min_lines: 40
    - path: "packages/mission-control/src/components/workspace/ProjectCardMenu.tsx"
      provides: "··· dropdown menu with three actions"
      min_lines: 30
  key_links:
    - from: "ProjectHomeScreen.tsx"
      to: "GET /api/projects/recent"
      via: "fetch on mount to load project list"
      pattern: "api/projects/recent"
    - from: "ProjectCardMenu.tsx"
      to: "PATCH /api/projects/recent/archive"
      via: "fetch on archive action"
      pattern: "api/projects/recent/archive"
    - from: "ProjectCard.tsx"
      to: "RecentProject"
      via: "props type import from server/fs-types"
      pattern: "RecentProject"
---

<objective>
Build the three React components that form the project home screen: ProjectHomeScreen (full-screen grid), ProjectCard (individual card), and ProjectCardMenu (··· menu). These are shown when no project is currently open.

Purpose: WORKSPACE-02 and WORKSPACE-03 — the managed home screen experience for both Builder and Developer users.
Output: Three TSX files in src/components/workspace/ with mode-aware empty states and full card rendering.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-project-workspace/19-RESEARCH.md
@packages/mission-control/src/server/fs-types.ts
@packages/mission-control/src/components/layout/AppShell.tsx
@packages/mission-control/src/components/session/OnboardingScreen.tsx

<interfaces>
<!-- Extended RecentProject type (after Plan 02): -->
```typescript
export interface RecentProject {
  path: string;
  name: string;
  lastOpened: number;    // Unix timestamp
  isGsdProject: boolean;
  archived?: boolean;
  activeMilestone?: string;
  progressPercent?: number;
  lastActivity?: string;
}
```

<!-- Design system tokens (established, do not deviate): -->
<!-- Background: #0F1419  Surface: #131C2B  Elevated: #1A2332  Border: #1E2D3D -->
<!-- Cyan: #5BC8F0  Green: #22C55E  Amber: #F59E0B  Red: #EF4444 -->
<!-- Fonts: Share Tech Mono (display), JetBrains Mono (body) -->
<!-- Spacing: 8-point grid -->

<!-- lucide-react icons available: MoreHorizontal, Archive, FolderOpen, Trash2, RotateCcw -->
```

<!-- ProjectHomeScreen callback contracts: -->
```typescript
interface ProjectHomeScreenProps {
  builderMode: boolean;
  onOpenProject: (path: string) => void;   // called when Resume clicked
  onOpenFolder: () => void;                // Developer mode: open folder picker
  onCreateProject: (name: string) => void; // Builder mode: create new project
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProjectCard + ProjectCardMenu components</name>
  <files>
    packages/mission-control/src/components/workspace/ProjectCard.tsx
    packages/mission-control/src/components/workspace/ProjectCardMenu.tsx
  </files>
  <behavior>
    ProjectCard:
    - Renders project name in Share Tech Mono
    - Renders relative timestamp ("2 hours ago", "3 days ago") from lastOpened
    - Renders activeMilestone if present (e.g. "v2.0 Native Desktop")
    - Renders progress bar filled to progressPercent% in cyan (#5BC8F0) on a dark track
    - Renders Resume button (cyan background, dark text) — calls onResume(project.path)
    - Renders ··· button top-right that opens ProjectCardMenu
    - Card background: surface (#131C2B), border: #1E2D3D, padding 16px, border-radius 6px

    ProjectCardMenu:
    - Three action items: Archive, Open in Finder/Explorer, Remove from list
    - Archive: calls onArchive(path) → PATCH /api/projects/recent/archive {path, archived: true}
    - Open in Finder/Explorer: calls Tauri IPC reveal_path(path) — import invoke from @tauri-apps/api/core; wrap in try/catch (no-op if not in Tauri context)
    - Remove from list: calls DELETE /api/projects/recent {path} — does NOT delete files
    - Menu is an absolute-positioned div (inline pattern, not a library component — per research anti-pattern guidance)
    - Closed by default, toggled by ··· button click
  </behavior>
  <action>
    Create packages/mission-control/src/components/workspace/ProjectCard.tsx:

    Props: { project: RecentProject; onResume: (path: string) => void; onArchive: (path: string) => void; onRemove: (path: string) => void; onRefresh: () => void }

    Relative time helper (inline, no library per research):
    ```typescript
    function relativeTime(ts: number): string {
      const diffMs = Date.now() - ts;
      const diffH = Math.floor(diffMs / 3600000);
      if (diffH < 1) return "just now";
      if (diffH < 24) return `${diffH} hour${diffH === 1 ? "" : "s"} ago`;
      const diffD = Math.floor(diffH / 24);
      return `${diffD} day${diffD === 1 ? "" : "s"} ago`;
    }
    ```

    Progress bar: `<div style={{ width: '100%', height: 6, background: '#1E2D3D', borderRadius: 3 }}><div style={{ width: `${project.progressPercent ?? 0}%`, height: '100%', background: '#5BC8F0', borderRadius: 3 }} /></div>`

    Import RecentProject from '@/server/fs-types'.
    Import ProjectCardMenu as a sub-component.

    Create packages/mission-control/src/components/workspace/ProjectCardMenu.tsx:

    Props: { path: string; onArchive: (path: string) => void; onRemove: (path: string) => void; onRefresh: () => void }

    Use useState for open/closed. Absolute positioned div below the ··· button.
    Icons from lucide-react: Archive, FolderOpen, Trash2 (16px).
    For reveal_path: `import { invoke } from '@tauri-apps/api/core'; invoke('reveal_path', { path }).catch(() => {})` — catch swallows error when not in Tauri context.
    For Archive action: fetch PATCH /api/projects/recent/archive, then call onRefresh() to reload list.
    For Remove: fetch DELETE /api/projects/recent with body { path }, then onRefresh().
    Menu item style: background #1A2332, hover #1E2D3D, text JetBrains Mono 12px, padding 8px 12px.
  </action>
  <verify>
    <automated>cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test tests/project-home-screen.test.tsx -t "ProjectCard renders" --timeout 5000 2>&1 | tail -10</automated>
  </verify>
  <done>
    ProjectCard and ProjectCardMenu test cases pass GREEN.
    Both files exist with correct exports.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProjectHomeScreen — grid, empty states, archived toggle</name>
  <files>
    packages/mission-control/src/components/workspace/ProjectHomeScreen.tsx
  </files>
  <behavior>
    - Developer mode (builderMode=false): empty state shows "Open Folder" button
    - Builder mode (builderMode=true): empty state shows project name input + "Create project" button
    - With projects: renders grid of ProjectCard components (filtered to archived===false or archived===undefined)
    - "Show archived" link at bottom — clicking shows archived projects in a separate section
    - Fetches GET /api/projects/recent on mount to load project list
    - After archive/remove action on a card, re-fetches to refresh the list
  </behavior>
  <action>
    Create packages/mission-control/src/components/workspace/ProjectHomeScreen.tsx:

    Props:
    ```typescript
    interface ProjectHomeScreenProps {
      builderMode: boolean;
      onOpenProject: (path: string) => void;
      onOpenFolder: () => void;           // Developer mode empty state
      onCreateProject: (name: string) => void; // Builder mode empty state
    }
    ```

    State:
    - projects: RecentProject[] (fetched from /api/projects/recent)
    - loading: boolean
    - showArchived: boolean (toggle for archived section)
    - newProjectName: string (Builder mode input)

    Layout:
    - Full-screen centered layout: background #0F1419
    - Header: "GSD Projects" in Share Tech Mono, 20px, cyan #5BC8F0
    - Grid: CSS grid, 3 columns on lg (min 340px each), 1 column on mobile, gap 16px
    - Main grid: projects where !project.archived
    - If no active projects: show mode-specific empty state
      - Developer: <button onClick={onOpenFolder}>Open Folder</button> (cyan border, cyan text)
      - Builder: <input placeholder="What are we building?" /> + <button onClick={() => onCreateProject(newProjectName)}>Create project</button>
    - "Show archived" link: shown at bottom only if any archived projects exist
    - Archived section (when showArchived): separate heading "Archived" with project cards that have a restore button (calls PATCH /api/projects/recent/archive {archived: false} + refresh)

    Import RecentProject from '@/server/fs-types'.
    Import ProjectCard from './ProjectCard'.

    useEffect on mount: fetch('/api/projects/recent').then(r => r.json()).then(setProjects).
  </action>
  <verify>
    <automated>cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test tests/project-home-screen.test.tsx --timeout 5000 2>&1 | tail -15</automated>
  </verify>
  <done>
    All project-home-screen.test.tsx cases pass GREEN (Developer empty state, Builder empty state, ProjectCard renders, ProjectCardMenu).
    ProjectHomeScreen.tsx exports a named component.
  </done>
</task>

</tasks>

<verification>
Run home screen tests:
`cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test tests/project-home-screen.test.tsx --timeout 5000`

Expected: All cases GREEN.

Run full suite:
`cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test --timeout 30000 2>&1 | tail -15`

Expected: 748+ previously passing tests still pass; project-home-screen.test.tsx now GREEN.
</verification>

<success_criteria>
- ProjectCard.tsx: renders name, relative time, milestone, progress bar, Resume button, ··· trigger
- ProjectCardMenu.tsx: Archive / Open in Finder / Remove from list; reveal_path IPC called for Open in Finder
- ProjectHomeScreen.tsx: mode-aware empty states, grid of non-archived cards, Show archived toggle
- project-home-screen.test.tsx: all test cases GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/19-project-workspace/19-03-SUMMARY.md`
</output>
