---
phase: 19-project-workspace
plan: 05
type: execute
wave: 4
depends_on: [19-04]
files_modified: []
autonomous: false
requirements: [WORKSPACE-01, WORKSPACE-02, WORKSPACE-03, WORKSPACE-04, WORKSPACE-05]

must_haves:
  truths:
    - "Full test suite passes at 748+ tests including all 4 new Phase 19 test files"
    - "With no project open the app shows ProjectHomeScreen grid"
    - "In Builder mode the home screen empty state shows a project name input, not an Open Folder button"
    - "In Developer mode the home screen shows an Open Folder button"
    - "Project cards render name, relative time, milestone, progress bar, and Resume button correctly"
    - "The ··· menu offers Archive, Open in Finder/Explorer, and Remove from list"
    - "Archiving a project removes it from the main grid; Show archived link restores visibility"
    - "The tab bar appears only when 2+ projects are in the open list"
  artifacts:
    - path: "packages/mission-control/src/components/workspace/ProjectHomeScreen.tsx"
      provides: "Home screen component"
    - path: "packages/mission-control/src/components/workspace/ProjectCard.tsx"
      provides: "Project card component"
    - path: "packages/mission-control/src/components/workspace/ProjectCardMenu.tsx"
      provides: "Card context menu"
    - path: "packages/mission-control/src/components/workspace/ProjectTabBar.tsx"
      provides: "Multi-project tab bar"
    - path: "packages/mission-control/src/server/workspace-api.ts"
      provides: "Workspace server module"
  key_links:
    - from: "AppShell.tsx"
      to: "ProjectHomeScreen"
      via: "mode === 'home'"
      pattern: "mode.*home"
    - from: "ProjectCardMenu"
      to: "PATCH /api/projects/recent/archive"
      via: "fetch on archive click"
      pattern: "archive"
---

<objective>
Gate Phase 19 completion with a full automated test suite run followed by human visual verification of all five WORKSPACE success criteria.

Purpose: Confirm that all implemented components function correctly end-to-end before Phase 19 is marked complete.
Output: Phase 19 approved; WORKSPACE-01 through WORKSPACE-05 satisfied.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-project-workspace/19-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite gate</name>
  <files></files>
  <action>
    Run the complete test suite and confirm all tests pass including the 4 new Phase 19 test files.

    Expected test count: 748 existing + ~12 new Phase 19 tests = 760+.

    If any tests fail:
    1. Identify which test file
    2. Read the failing test and the implementation file it references
    3. Fix the implementation (not the test — tests are the spec)
    4. Re-run until the full suite is green

    Do not proceed to Task 2 until the full suite is clean.
  </action>
  <verify>
    <automated>cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test --timeout 30000 2>&1 | tail -20</automated>
  </verify>
  <done>Full test suite GREEN. 760+ tests pass. Zero failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Project Workspace system:
    - ProjectHomeScreen shown when no project is open (home mode)
    - Mode-aware empty states: Builder mode shows project name input; Developer mode shows Open Folder
    - Project cards with name, last active time, milestone, progress bar, Resume button
    - ··· menu with Archive, Open in Finder/Explorer, Remove from list
    - Archiving moves project out of main grid; Show archived link reveals archived projects
    - ProjectTabBar appears when 2+ projects are open, with amber dot for executing
    - Sidebar has Home button to return to ProjectHomeScreen
  </what-built>
  <how-to-verify>
    Start the dev server: `cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun dev`

    SC-1 — Home screen shown with no project open:
    1. Open http://localhost:4000 with no project active
    2. Verify ProjectHomeScreen is shown (not the onboarding screen)
    3. In Developer mode: confirm "Open Folder" button visible
    4. Switch to Builder mode in Settings, navigate to home via the Home button in sidebar
    5. Confirm project name input is shown instead of Open Folder

    SC-2 — Project card content:
    1. If you have a recent project in ~/.gsd/recent-projects.json, verify card shows:
       - Project name, relative time (e.g. "2 hours ago"), Resume button
       - If activeMilestone/progressPercent fields populated: milestone name and progress bar
    2. Click Resume on a project card — verify project loads into dashboard view

    SC-3 — ··· menu:
    1. Click the ··· button on a project card
    2. Verify three menu items appear: Archive, Open in Finder/Explorer, Remove from list
    3. Click Archive — verify project disappears from main grid
    4. Click "Show archived" link — verify project reappears in Archived section
    5. Click the restore icon/button on an archived project — verify it returns to main grid

    SC-4 — Multi-project tab bar:
    1. Open one project (Resume from home screen) — verify NO tab bar appears
    2. Click the Home button in the sidebar — return to home screen
    3. Open a second project — verify a tab bar appears at top with both project names
    4. Click the first project's tab — verify it becomes active (cyan highlight or border)

    SC-5 — Builder mode new project:
    1. In Builder mode, from the home screen, enter a project name in the input
    2. Click Create project
    3. Verify the project directory was created under ~/GSD Projects/ (check filesystem)
    4. Verify .git/ directory exists inside the new project folder
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found in any SC</resume-signal>
</task>

</tasks>

<verification>
Full suite gate:
`cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test --timeout 30000 2>&1 | tail -5`

Phase 19 requirements covered:
- WORKSPACE-01: workspace-api.test.ts GREEN + SC-5 human verify
- WORKSPACE-02: project-home-screen.test.tsx GREEN + SC-1 human verify
- WORKSPACE-03: project-home-screen.test.tsx GREEN + SC-2 + SC-3 human verify
- WORKSPACE-04: project-tab-bar.test.tsx GREEN + SC-4 human verify
- WORKSPACE-05: project-archiving.test.ts GREEN + SC-3 human verify
</verification>

<success_criteria>
- Full test suite GREEN (760+ tests)
- Human verification SC-1 through SC-5 approved
- All five WORKSPACE requirements satisfied
- Phase 19 ready to be marked complete in STATE.md and ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/19-project-workspace/19-05-SUMMARY.md`
</output>
