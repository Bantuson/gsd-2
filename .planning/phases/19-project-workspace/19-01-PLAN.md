---
phase: 19-project-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mission-control/tests/workspace-api.test.ts
  - packages/mission-control/tests/project-home-screen.test.tsx
  - packages/mission-control/tests/project-tab-bar.test.tsx
  - packages/mission-control/tests/project-archiving.test.ts
autonomous: true
requirements: [WORKSPACE-01, WORKSPACE-02, WORKSPACE-03, WORKSPACE-04, WORKSPACE-05]

must_haves:
  truths:
    - "Four test files exist with failing (RED) stubs — bun test reports failures not 'no tests found'"
    - "Each test file covers exactly the behaviors listed in 19-VALIDATION.md"
    - "Full test suite still passes (748+) — stubs do not break existing tests"
  artifacts:
    - path: "packages/mission-control/tests/workspace-api.test.ts"
      provides: "Failing stubs for WORKSPACE-01 (getWorkspacePath, createProject, workspace_path setting)"
    - path: "packages/mission-control/tests/project-home-screen.test.tsx"
      provides: "Failing stubs for WORKSPACE-02, WORKSPACE-03 (ProjectHomeScreen, ProjectCard, ProjectCardMenu)"
    - path: "packages/mission-control/tests/project-tab-bar.test.tsx"
      provides: "Failing stubs for WORKSPACE-04 (tab visibility, amber dot)"
    - path: "packages/mission-control/tests/project-archiving.test.ts"
      provides: "Failing stubs for WORKSPACE-05 (archive, show archived, restore)"
  key_links:
    - from: "tests/workspace-api.test.ts"
      to: "src/server/workspace-api.ts"
      via: "import { getWorkspacePath, createProject } from '../src/server/workspace-api'"
      pattern: "workspace-api"
    - from: "tests/project-archiving.test.ts"
      to: "src/server/recent-projects.ts"
      via: "import { archiveProject, restoreProject } from '../src/server/recent-projects'"
      pattern: "archiveProject|restoreProject"
---

<objective>
Create four failing test stub files that define the expected behavior of Phase 19's new modules before any implementation exists.

Purpose: Nyquist compliance — every automated verify command in the phase must reference an existing test file. These stubs will turn GREEN as implementation plans execute.
Output: Four test files with RED stubs; full suite (748+) still passes.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-project-workspace/19-VALIDATION.md
@.planning/phases/19-project-workspace/19-RESEARCH.md
@packages/mission-control/src/server/fs-types.ts
@packages/mission-control/src/server/recent-projects.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: workspace-api and project-archiving test stubs</name>
  <files>
    packages/mission-control/tests/workspace-api.test.ts
    packages/mission-control/tests/project-archiving.test.ts
  </files>
  <behavior>
    workspace-api.test.ts:
    - Test: "getWorkspacePath returns ~/GSD Projects on non-Windows" — import getWorkspacePath, set process.platform mock to 'linux', assert result contains "GSD Projects"
    - Test: "getWorkspacePath returns USERPROFILE/GSD Projects on Windows" — set process.platform to 'win32', assert USERPROFILE-based path
    - Test: "workspace_path setting stored and read" — import setWorkspacePath/getWorkspacePath with override, call setWorkspacePath('/custom'), call getWorkspacePath('/custom'), assert '/custom'
    - Test: "createProject creates directory and runs git init" — call createProject with a temp dir + name, assert returned projectPath exists, assert .git/ subdirectory exists within projectPath

    project-archiving.test.ts:
    - Test: "archive sets archived true and project disappears from main grid" — call archiveProject(path), call getRecentProjects(), assert project with that path has archived: true
    - Test: "show archived returns archived projects" — call getArchivedProjects(), assert archived project appears
    - Test: "restore sets archived false and returns project to main grid" — call restoreProject(path), call getRecentProjects(), assert project archived is false or absent
  </behavior>
  <action>
    Create tests/workspace-api.test.ts:
    - Import from '../src/server/workspace-api' (file does NOT exist yet — import will fail with module error = RED)
    - Use bun:test describe/it pattern matching existing test files (see tests/watcher.test.ts for style)
    - Use _setWorkspaceFilePath (test override) pattern matching _setRecentFilePath in recent-projects.ts
    - For createProject test: use import { tmpdir } from 'node:os' and import { rm } from 'node:fs/promises' for cleanup in afterEach
    - Stubs for functions: getWorkspacePath(overridePath?: string): string, createProject(name: string, workspacePath: string): Promise<{projectPath: string}>, setWorkspacePath is handled via settings-api pattern

    Create tests/project-archiving.test.ts:
    - Import archiveProject, restoreProject, getArchivedProjects from '../src/server/recent-projects' (these do not exist yet = RED)
    - Import _setRecentFilePath (already exported) for test isolation — use tmpdir-based temp file
    - archiveProject(path: string): Promise<void> — sets archived: true on the matching entry
    - restoreProject(path: string): Promise<void> — sets archived: false on the matching entry
    - getArchivedProjects(): Promise<RecentProject[]> — returns entries where archived === true
    - afterEach: rm temp file
  </action>
  <verify>
    <automated>cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test tests/workspace-api.test.ts tests/project-archiving.test.ts --timeout 5000 2>&1 | tail -20</automated>
  </verify>
  <done>Tests exist and fail with import errors or assertion failures (RED). No syntax errors in test files themselves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: project-home-screen and project-tab-bar test stubs</name>
  <files>
    packages/mission-control/tests/project-home-screen.test.tsx
    packages/mission-control/tests/project-tab-bar.test.tsx
  </files>
  <behavior>
    project-home-screen.test.tsx:
    - Test: "Developer empty state shows Open Folder button" — render ProjectHomeScreen with builderMode=false, assert source text contains "Open Folder" (use source-text strategy from Phase 12 decisions)
    - Test: "Builder empty state shows brief-taking input" — render with builderMode=true, assert source contains project name input or brief input placeholder
    - Test: "ProjectCard renders name, last active, milestone, progress, Resume button" — render ProjectCard with fixture data, assert name/lastOpened/activeMilestone/"Resume" appear in output
    - Test: "ProjectCardMenu shows Archive, Open in Finder, Remove from list" — render ProjectCardMenu, assert three menu items visible

    project-tab-bar.test.tsx:
    - Test: "tab bar hidden when 0 projects open" — render ProjectTabBar with openProjects=[], assert component renders null or empty
    - Test: "tab bar hidden when 1 project open" — render with 1 project, assert not visible (single-project tab bar is not shown)
    - Test: "tab bar visible when 2+ projects open" — render with 2 projects, assert tab bar renders both names
    - Test: "amber dot shown when project isProcessing" — render with project where isProcessing=true, assert amber indicator present in source
  </behavior>
  <action>
    Create tests/project-home-screen.test.tsx:
    - Import ProjectHomeScreen from '../src/components/workspace/ProjectHomeScreen' (does not exist = RED)
    - Import ProjectCard from '../src/components/workspace/ProjectCard'
    - Import ProjectCardMenu from '../src/components/workspace/ProjectCardMenu'
    - Use static source-text strategy (Phase 12 decision): import { readFileSync } from 'node:fs' and read the TSX source file as a string for the more complex UI assertions. For simpler assertions, use renderToString from 'react-dom/server' (Phase 11.1-03 decision: direct class instance testing for react-dom/server)
    - RecentProject fixture with new fields: { path: '/projects/test', name: 'Test Project', lastOpened: Date.now() - 7200000, isGsdProject: true, archived: false, activeMilestone: 'v2.0', progressPercent: 43, lastActivity: '2026-03-14' }

    Create tests/project-tab-bar.test.tsx:
    - Import ProjectTabBar from '../src/components/workspace/ProjectTabBar' (does not exist = RED)
    - Use renderToString for HTML output checks
    - OpenProject type (inline in test): { id: string, name: string, isProcessing: boolean }
  </action>
  <verify>
    <automated>cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test tests/project-home-screen.test.tsx tests/project-tab-bar.test.tsx --timeout 5000 2>&1 | tail -20</automated>
  </verify>
  <done>Tests exist and fail with import/module errors (RED). Source files for components do not exist yet.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full suite still passes</name>
  <files></files>
  <action>
    Run the full test suite to confirm that adding 4 new test files with RED stubs has not broken existing 748+ tests. The new test failures are expected and acceptable — the concern is that no previously-passing test has regressed.

    If the full suite shows new failures beyond the 4 new stub test files, investigate and fix before this plan completes.
  </action>
  <verify>
    <automated>cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test --timeout 30000 2>&1 | tail -30</automated>
  </verify>
  <done>All previously passing tests still pass. New failures are only in the 4 stub test files (workspace-api, project-home-screen, project-tab-bar, project-archiving).</done>
</task>

</tasks>

<verification>
Run the four stub test files:
`cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test tests/workspace-api.test.ts tests/project-home-screen.test.tsx tests/project-tab-bar.test.tsx tests/project-archiving.test.ts --timeout 5000`

Expected: All fail (RED) — module not found or assertion failures.

Run full suite:
`cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test --timeout 30000`

Expected: 748+ previously passing tests still pass; only new stub files fail.
</verification>

<success_criteria>
- Four test files created in packages/mission-control/tests/
- Each test file targets a function or component that does not yet exist (RED state)
- Test names exactly match the `-t` filter patterns used in 19-VALIDATION.md
- Full suite passes (748+) minus the 4 new stub files
</success_criteria>

<output>
After completion, create `.planning/phases/19-project-workspace/19-01-SUMMARY.md`
</output>
