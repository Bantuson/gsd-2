---
phase: 20.2.3-security-hardening-and-pr-check-validation
plan: 02
subsystem: api
tags: [security, bun, typescript, path-traversal, ssrf, session, input-validation]

# Dependency graph
requires: []
provides:
  - "C3: trust-api validates dir must be a .gsd path (POST and GET)"
  - "C4: proxy-api validates port range 1024-65535 and strips Authorization/Cookie headers"
  - "H6: server.ts static serving checks filePath.startsWith(publicDir) before serving"
  - "H3/H4: server.ts SECURITY NOTE documenting accepted HTTP threat model"
  - "C1/C2: fs-api SECURITY NOTE documenting intentional browsing design + mkdir homedir restriction"
  - "H7: git-api SECURITY NOTE + isAbsolute check rejecting relative root paths"
  - "M: auth-api uses crypto.randomUUID() instead of Math.random() for session IDs"
affects: [20.2.3-03, pr-1500-security-audit]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "SECURITY NOTE comment blocks following ws-server.ts pattern for accepted threat model documentation"
    - "Port range validation (1024-65535) on proxy to prevent SSRF to privileged services"
    - "Header stripping (Authorization, Cookie) before forwarding proxy requests"
    - "Path containment check using startsWith() after resolve() for traversal prevention"
    - "Home directory restriction for mkdir using homedir().toLowerCase() comparison"
    - "crypto.randomUUID() for cryptographically secure session ID generation"

key-files:
  created: []
  modified:
    - packages/mission-control/src/server/trust-api.ts
    - packages/mission-control/src/server/proxy-api.ts
    - packages/mission-control/src/server.ts
    - packages/mission-control/src/server/fs-api.ts
    - packages/mission-control/src/server/git-api.ts
    - packages/mission-control/src/server/auth-api.ts
    - packages/mission-control/tests/trust-api.test.ts

key-decisions:
  - "C3 validation applied to both GET and POST trust routes — consistency over convenience"
  - "Proxy returns offline HTML (200) for invalid ports rather than 4xx — matches existing offline behavior UX"
  - "git-api allows root parameter to be any absolute path (accepted threat model) but rejects relative paths"
  - "mkdir restriction uses case-insensitive path comparison for Windows compatibility"

patterns-established:
  - "SECURITY NOTE pattern: copy ws-server.ts format (Intentional: / reason / numbered rationale / upgrade note)"
  - "Path traversal guard: resolve() then startsWith(resolvedBase) — always after resolve to handle .. segments"
  - "Proxy header stripping: new Headers(req.headers) then delete() before forwarding"

requirements-completed: [C1, C2, C3, C4, H6, H7, M-RANDOM]

# Metrics
duration: 7min
completed: 2026-03-20
---

# Phase 20.2.3 Plan 02: Security API Hardening Summary

**Server-side security hardening: path traversal prevention, SSRF port validation, credential header stripping, homedir mkdir restriction, and crypto.randomUUID() session IDs across all Bun HTTP API endpoints.**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-03-20T08:38:44Z
- **Completed:** 2026-03-20T08:45:52Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments

- C3/C4/H6: trust-api, proxy-api, and server.ts hardened — dir validation, port range check, header stripping, and traversal guard
- C1/C2/H7/M: fs-api, git-api, and auth-api hardened — SECURITY NOTEs, mkdir homedir restriction, absolute path check, and cryptographically secure session IDs
- All 1045 tests pass after updating trust-api tests to use .gsd paths per the new C3 validation

## Task Commits

Each task was committed atomically:

1. **Task 1: Harden trust-api, proxy-api, and server.ts static serving** - `8f8c6c4` (feat)
2. **Task 2: Add security notes to fs-api and git-api, fix mkdir restriction, replace Math.random in auth-api** - `a81840e` (feat)

**Plan metadata:** (committed after self-check)

## Files Created/Modified

- `packages/mission-control/src/server/trust-api.ts` - C3: dir must contain /.gsd in GET and POST routes
- `packages/mission-control/src/server/proxy-api.ts` - C4: port 1024-65535 validation; Authorization/Cookie header stripping
- `packages/mission-control/src/server.ts` - publicDir const; H6 traversal guard; H3/H4 SECURITY NOTE
- `packages/mission-control/src/server/fs-api.ts` - C1/C2 SECURITY NOTE; mkdir homedir restriction
- `packages/mission-control/src/server/git-api.ts` - H7 SECURITY NOTE; isAbsolute import; relative path rejection
- `packages/mission-control/src/server/auth-api.ts` - M: crypto.randomUUID() replaces Math.random() for sessionId
- `packages/mission-control/tests/trust-api.test.ts` - Updated tests to use .gsd paths; added 400 validation tests

## Decisions Made

- C3 validation applied to both GET and POST trust routes for consistency
- Proxy returns offline HTML (200) for invalid ports — matches existing offline page UX (not a 4xx error)
- git-api root parameter accepted as any absolute path (read-only operation, accepted threat model) but relative paths rejected
- mkdir uses case-insensitive comparison for Windows compatibility

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated trust-api tests to use .gsd directory paths**
- **Found during:** Task 2 post-task test run
- **Issue:** Existing `registerTrustRoutes` tests passed bare temp directories (e.g., `/tmp/trust-rest-XXXX`) which now correctly fail the C3 validation added in Task 1
- **Fix:** Updated two existing tests to append `/.gsd` to temp dir path; added two new tests that verify the 400 response for non-.gsd paths
- **Files modified:** `packages/mission-control/tests/trust-api.test.ts`
- **Verification:** `bun test tests/trust-api.test.ts` — 9 pass, 0 fail; full suite 1045 pass, 0 fail
- **Committed in:** `a81840e` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — bug: tests broken by new validation)
**Impact on plan:** The fix was necessary to maintain test coverage after adding the validation. The new tests also improve coverage by explicitly testing the 400 rejection path.

## Issues Encountered

None — both tasks executed cleanly once tests were updated for the new validation behavior.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All C1-C4, H6, H7, and M-RANDOM findings from @glittercowboy's audit are addressed
- 1045 tests pass with no regressions
- Ready for Plan 03 (CI test fixes / remaining findings) and the post-phase gate (security re-audit, push)

---
*Phase: 20.2.3-security-hardening-and-pr-check-validation*
*Completed: 2026-03-20*
