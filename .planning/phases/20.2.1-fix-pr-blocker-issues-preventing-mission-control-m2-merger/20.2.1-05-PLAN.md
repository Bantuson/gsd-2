---
phase: 20.2.1-fix-pr-blocker-issues-preventing-mission-control-m2-merger
plan: 05
type: execute
wave: 2
depends_on: ["20.2.1-01", "20.2.1-02", "20.2.1-04"]
files_modified:
  - .github/workflows/pr-check.yml
autonomous: true
requirements: [S1]

must_haves:
  truths:
    - "pr-check.yml unit test step does NOT have continue-on-error: true"
    - "Full test suite passes (all fixes from Plans 01-04 are in)"
    - "cargo check passes with all Rust fixes from Plans 01-02"
  artifacts:
    - path: ".github/workflows/pr-check.yml"
      provides: "CI unit test step without continue-on-error"
  key_links:
    - from: ".github/workflows/pr-check.yml unit tests step"
      to: "bun test exit code"
      via: "Step fails the job if tests fail (no error suppression)"
      pattern: "bun test --cwd packages/mission-control"
---

<objective>
Remove continue-on-error from CI unit tests and run full verification gate across all fixes.

Purpose: S1 removes the safety net that lets broken tests pass CI. This plan runs last to verify all prior fixes are complete.
Output: Updated pr-check.yml, full suite green confirmation.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.2.1-fix-pr-blocker-issues-preventing-mission-control-m2-merger/20.2.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove continue-on-error from pr-check.yml and run full verification</name>
  <files>.github/workflows/pr-check.yml</files>
  <read_first>
    - .github/workflows/pr-check.yml
  </read_first>
  <action>
1. Edit `.github/workflows/pr-check.yml` line 56.

   Remove the entire line:
   ```yaml
        continue-on-error: true   # don't block PR on pre-existing test failures
   ```

   The "Unit tests" step should look like:
   ```yaml
      - name: Unit tests
        run: bun test --cwd packages/mission-control
   ```

   No blank line should remain where the removed line was.

2. After saving, run the full verification gate:
   - `bun test --cwd packages/mission-control` — all tests must pass (0 failures)
   - `cargo check --manifest-path packages/mission-control/src-tauri/Cargo.toml` — must exit 0

These commands verify that all fixes from Plans 01-04 are correct and the test suite is fully green before the CI safety net is removed.
  </action>
  <acceptance_criteria>
    - pr-check.yml does NOT contain `continue-on-error`
    - pr-check.yml contains `bun test --cwd packages/mission-control` (the test command itself is preserved)
    - `bun test --cwd packages/mission-control` exits 0 with 0 failures
    - `cargo check --manifest-path packages/mission-control/src-tauri/Cargo.toml` exits 0
  </acceptance_criteria>
  <verify>
    <automated>bun test --cwd packages/mission-control 2>&1 | tail -5</automated>
  </verify>
  <done>continue-on-error removed from pr-check.yml, full test suite passes, cargo check passes — all 8 blocker/suggestion items resolved</done>
</task>

</tasks>

<verification>
- `grep -c "continue-on-error" .github/workflows/pr-check.yml` returns 0
- `bun test --cwd packages/mission-control` — full suite green
- `cargo check --manifest-path packages/mission-control/src-tauri/Cargo.toml` — clean
</verification>

<success_criteria>
- pr-check.yml has no continue-on-error on the unit test step
- All tests pass across the full suite
- All 8 items (B1-B6, S1, S2) are resolved
</success_criteria>

<output>
After completion, create `.planning/phases/20.2.1-fix-pr-blocker-issues-preventing-mission-control-m2-merger/20.2.1-05-SUMMARY.md`
</output>
