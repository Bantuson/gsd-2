---
phase: 20.2.1
slug: fix-pr-blocker-issues-preventing-mission-control-m2-merger
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-19
---

# Phase 20.2.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Bun test (built-in) |
| **Config file** | none — `bun test` autodiscovers `tests/*.test.ts` |
| **Quick run command** | `bun test --cwd packages/mission-control 2>&1 \| tail -5` |
| **Full suite command** | `bun test --cwd packages/mission-control` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `bun test --cwd packages/mission-control 2>&1 | tail -5`
- **After every plan wave:** Run `bun test --cwd packages/mission-control`
- **Before `/gsd:verify-work`:** Full suite green + `cargo check --release` clean
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| B2-fix | 01 | 1 | B2 open_external URL scheme validation | source-text/unit | `bun test tests/security-commands.test.ts` | ❌ Wave 0 | pending |
| B3-fix | 01 | 1 | B3 credential key allowlist | source-text/unit | `bun test tests/security-commands.test.ts` | ❌ Wave 0 | pending |
| B5-fix | 02 | 1 | B5 tokio::time::sleep in watch_bun_process | source-text | `bun test tests/tauri-bun-ipc.test.ts` | ✅ (partial) | pending |
| S2-fix | 02 | 1 | S2 start script in package.json | source-text | `bun test tests/production-start.test.ts` | ❌ Wave 0 | pending |
| B1-fix | 03 | 1 | B1 Tauri updater pubkey non-empty | source-text | `bun test tests/tauri-scaffold.test.ts` | ✅ | pending |
| B4-fix | 04 | 1 | B4 no_project_loaded gate | source-text/unit | `bun test tests/project-gate.test.ts` | ❌ Wave 0 | pending |
| B6-fix | 04 | 1 | B6 addCorsHeaders Response reconstruction | source-text | `bun test tests/server-cors.test.ts` | ✅ (needs update) | pending |
| S1-fix | 05 | 2 | S1 continue-on-error removed | source-text | `bun test tests/release-workflow.test.ts` | ✅ (partial) | pending |

---

## Wave 0 Gaps (must be created before implementation)

- [ ] `packages/mission-control/tests/security-commands.test.ts` — B2 (open_external scheme check) + B3 (credential key allowlist)
- [ ] `packages/mission-control/tests/project-gate.test.ts` — B4 (no_project_loaded emission, chat rejection before project selected)
- [ ] `packages/mission-control/tests/production-start.test.ts` — S2 (start script present, bun_manager uses "start" not "dev")

---

## Phase Gate

All of the following must pass before `/gsd:verify-work`:

1. `bun test --cwd packages/mission-control` → 0 failures (all 8 requirement tests green)
2. `cargo check --cwd packages/mission-control/src-tauri` → 0 errors
3. `tauri.conf.json` pubkey field is non-empty string
4. `pr-check.yml` does NOT contain `continue-on-error` on the unit test step
5. `packages/mission-control/package.json` scripts contains `"start"` entry
6. `bun_manager.rs` does NOT contain `std::thread::sleep` (both locations fixed)

---

## Requirements Coverage

| Requirement | Covering Plan | Status |
|-------------|--------------|--------|
| B1 — Tauri updater pubkey | Plan 03 | ○ |
| B2 — open_external URL scheme | Plan 01 | ○ |
| B3 — credential key allowlist | Plan 01 | ○ |
| B4 — no_project_loaded gate | Plan 04 | ○ |
| B5 — tokio::time::sleep | Plan 02 | ○ |
| B6 — CORS headers reconstruction | Plan 04 | ○ |
| S1 — remove continue-on-error | Plan 05 | ○ |
| S2 — production start script | Plan 02 | ○ |
