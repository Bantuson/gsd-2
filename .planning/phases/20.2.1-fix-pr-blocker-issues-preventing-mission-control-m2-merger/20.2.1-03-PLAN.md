---
phase: 20.2.1-fix-pr-blocker-issues-preventing-mission-control-m2-merger
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mission-control/src-tauri/tauri.conf.json
autonomous: false
requirements: [B1]

user_setup:
  - service: tauri-signer
    why: "Updater pubkey generation for signing verification"
    env_vars:
      - name: TAURI_SIGNING_PRIVATE_KEY
        source: "Output of cargo tauri signer generate — the private key line"
    dashboard_config:
      - task: "Store private key in GitHub Actions secret TAURI_SIGNING_PRIVATE_KEY"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions"

must_haves:
  truths:
    - "tauri.conf.json has a non-empty pubkey string in plugins.updater.pubkey"
    - "The pubkey is a valid base64-encoded Ed25519 public key generated by cargo tauri signer"
    - "The private key is communicated to the user for GitHub Actions secret setup"
  artifacts:
    - path: "packages/mission-control/src-tauri/tauri.conf.json"
      provides: "Updater config with real pubkey"
      contains: "pubkey"
  key_links:
    - from: "tauri.conf.json updater pubkey"
      to: "release.yml TAURI_SIGNING_PRIVATE_KEY"
      via: "Key pair — pubkey in config, private key in CI secret"
      pattern: '"pubkey": ".+"'
---

<objective>
Generate a Tauri updater signing key pair and embed the public key in tauri.conf.json (B1).

Purpose: Without a real pubkey, the updater accepts any payload — enabling MITM attacks on auto-update.
Output: tauri.conf.json with valid pubkey, user instructed to store private key in GitHub Actions.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.2.1-fix-pr-blocker-issues-preventing-mission-control-m2-merger/20.2.1-CONTEXT.md
@.planning/phases/20.2.1-fix-pr-blocker-issues-preventing-mission-control-m2-merger/20.2.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Tauri signer key pair and embed pubkey</name>
  <files>packages/mission-control/src-tauri/tauri.conf.json</files>
  <read_first>
    - packages/mission-control/src-tauri/tauri.conf.json
  </read_first>
  <action>
1. Run `cargo tauri signer generate -w ~/.tauri/gsd-mc.key` to generate a key pair. This writes the private key to `~/.tauri/gsd-mc.key` and prints the public key to stdout.

   If the command prompts for a password, press Enter twice for no password (acceptable for CI use).

   The output will contain a line like:
   ```
   Your public key: dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIH...
   ```

2. Copy the public key string (the base64 value after "Your public key: ").

3. Edit `packages/mission-control/src-tauri/tauri.conf.json` line 71:

   Change:
   ```json
   "pubkey": "",
   ```
   To:
   ```json
   "pubkey": "<the-actual-base64-public-key-from-step-1>",
   ```

   The pubkey must be the EXACT string output by the tool — no newlines, no extra whitespace.

4. Record the private key location (`~/.tauri/gsd-mc.key`) for the checkpoint task. The user must add this to GitHub Actions secret `TAURI_SIGNING_PRIVATE_KEY`.
  </action>
  <acceptance_criteria>
    - tauri.conf.json `plugins.updater.pubkey` field is a non-empty string (not `""`)
    - The pubkey value is at least 40 characters long (base64 Ed25519 keys are ~88+ chars)
    - tauri.conf.json is valid JSON (parseable by `node -e "JSON.parse(require('fs').readFileSync('...', 'utf-8'))"`)
    - File `~/.tauri/gsd-mc.key` exists (private key was saved)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const c=JSON.parse(require('fs').readFileSync('packages/mission-control/src-tauri/tauri.conf.json','utf-8')); const pk=c.plugins.updater.pubkey; console.log('pubkey length:', pk.length); if(pk.length < 40) { console.error('FAIL: pubkey too short or empty'); process.exit(1); } console.log('PASS: pubkey is non-empty')"</automated>
  </verify>
  <done>tauri.conf.json has a real Ed25519 pubkey in plugins.updater.pubkey field, private key saved locally</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User must store private key in GitHub Actions secret</name>
  <files>none</files>
  <action>Present the private key location and instructions to the user. The private key was saved at ~/.tauri/gsd-mc.key by the signer generate command in Task 1.</action>
  <what-built>Generated Tauri signing key pair. The public key is committed in tauri.conf.json. The private key is saved at ~/.tauri/gsd-mc.key.</what-built>
  <how-to-verify>
    1. Read the private key: `cat ~/.tauri/gsd-mc.key`
    2. Go to GitHub repo Settings -> Secrets and variables -> Actions
    3. Create/update secret `TAURI_SIGNING_PRIVATE_KEY` with the private key contents
    4. Verify the secret name matches what release.yml expects: `TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}`
  </how-to-verify>
  <verify>User confirms the GitHub Actions secret has been created</verify>
  <done>Private key stored in GitHub Actions secret TAURI_SIGNING_PRIVATE_KEY</done>
  <resume-signal>Type "done" once the GitHub Actions secret is set, or "skip" to defer this to later</resume-signal>
</task>

</tasks>

<verification>
- `node -e "..."` confirms pubkey is non-empty and at least 40 chars
- tauri.conf.json is valid JSON
- Private key file exists at ~/.tauri/gsd-mc.key
</verification>

<success_criteria>
- tauri.conf.json pubkey field contains a real base64 Ed25519 public key
- Private key is saved locally and user is instructed to add it to GitHub Actions
</success_criteria>

<output>
After completion, create `.planning/phases/20.2.1-fix-pr-blocker-issues-preventing-mission-control-m2-merger/20.2.1-03-SUMMARY.md`
</output>
