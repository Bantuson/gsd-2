---
phase: 20.2.5-security-behaviour-closure
plan: "03"
subsystem: security-tests
tags: [security, xss, credentials, dompurify, jsdom, testing, red-baseline]
dependency_graph:
  requires: []
  provides:
    - RED baseline for T-XSS (B65-B70) and T-CRED (B71-B76)
    - Holistic DOMPurify DOM inspection test suite
    - Holistic trust fail-closed and fetch monkey-patch test suite
  affects:
    - packages/mission-control/tests/security-txss.test.ts
    - packages/mission-control/tests/security-tcred.test.ts
tech_stack:
  added:
    - dompurify@3.3.3 (devDependency)
    - jsdom@29.0.1 (devDependency)
    - "@types/dompurify@3.2.0" (devDependency)
    - "@types/jsdom@28.0.1" (devDependency)
  patterns:
    - DOMPurify + jsdom for holistic DOM sanitization testing
    - Static config checks on tauri.conf.json and capability JSON files
    - Direct module import for trust-api.ts functional testing
key_files:
  created: []
  modified:
    - packages/mission-control/tests/security-txss.test.ts
    - packages/mission-control/tests/security-tcred.test.ts
    - packages/mission-control/package.json
decisions:
  - "B69/B70 tests are correctly RED: commands.rs uses raw format!(\"file://{}\", path) in reveal_path fallback — remediation needed in Wave 3"
  - "B72 test is correctly RED: main.json lacks explicit set_credential/get_credential/delete_credential permissions — these are registered via lib.rs but not in capability JSON"
  - "B73 test targets both isTrusted() (GREEN) and App.tsx fail-open pattern (RED) — App.tsx has explicit .catch(() => setTrustStatus(\"trusted\")) comment"
metrics:
  duration: "~10 minutes"
  completed: "2026-03-24"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 3
---

# Phase 20.2.5 Plan 03: T-XSS and T-CRED Holistic Security Tests Summary

Rewrote security-txss.test.ts (B65-B70) and security-tcred.test.ts (B71-B76) with holistic behaviour-based testing using DOMPurify + jsdom DOM inspection, direct module import for functional testing, and permitted static config checks on tauri.conf.json and capability JSON.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Rewrite security-txss.test.ts with holistic DOM inspection tests (B65-B70) | 05af5717 | tests/security-txss.test.ts, package.json |
| 2 | Rewrite security-tcred.test.ts with holistic trust and fetch tests (B71-B76) | a4b8868e | tests/security-tcred.test.ts |

## Test Results — RED Baseline Established

### T-XSS Tests (security-txss.test.ts)

| B | Test | Result | Notes |
|---|------|--------|-------|
| B65 | DOMPurify removes script tags, javascript: URLs, onerror handlers | PASS (GREEN) | DOMPurify + jsdom library test works correctly |
| B66 | javascript: links do not survive sanitization | PASS (GREEN) | DOMPurify strips javascript: hrefs |
| B67 | CSP connect-src no wildcard ws://127.0.0.1:* | FAIL (RED) | tauri.conf.json has `ws://127.0.0.1:*` and `ws://localhost:*` |
| B68 | OAuthConnectFlow exports validateOAuthUrl | FAIL (RED) | Not yet exported from OAuthConnectFlow.tsx |
| B69 | commands.rs uses Url::parse (not raw format!) | FAIL (RED) | reveal_path uses `format!("file://{}", path)` |
| B70 | file:// URL construction via URL parser | FAIL (RED) | Same raw format! pattern in reveal_path fallback |

### T-CRED Tests (security-tcred.test.ts)

| B | Test | Result | Notes |
|---|------|--------|-------|
| B71 | No plaintext API keys in auth.json | PASS (GREEN) | No auth.json present on this machine |
| B72 | Credential IPC restricted to main.json only | FAIL (RED) | main.json lacks set_credential/get_credential/delete_credential permissions |
| B73 | Trust check fails closed on error | FAIL (RED) | App.tsx has `.catch(() => setTrustStatus("trusted"))` — explicit fail-open |
| B74 | TrustDialog requires explicit confirmation | PASS (GREEN) | TrustDialog.tsx has handleConfirm — regression test passes |
| B75 | Fetch monkey-patch handles Headers instance and array | FAIL (RED) | window-identity.ts uses spread without instanceof Headers check |
| B76 | Asset scope not $APP/** wildcard | FAIL (RED) | tauri.conf.json has `"$APP/**"` in assetScope.allow |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] dompurify and jsdom not installed**
- **Found during:** Task 1 execution
- **Issue:** Plan 01 Task 3 was supposed to install dompurify, jsdom, @types/dompurify, @types/jsdom but they were absent from node_modules and package.json (parallel wave execution — plans run concurrently)
- **Fix:** Ran `bun add -d dompurify jsdom @types/dompurify @types/jsdom` in packages/mission-control
- **Files modified:** packages/mission-control/package.json
- **Committed in:** 05af5717

### B72 Test Adjustment

The B72 test from the plan used a `await import("node:fs")` inside a non-async `it()` block, which fails with "await can only be used inside an async function". Fixed by adding `readdirSync` to the top-level import statement and removing the dynamic import.

## Known Stubs

None — this plan creates test files only. All tests are either correctly passing (GREEN) or correctly failing (RED) per the intended baseline.

## Self-Check: PASSED

- [x] `packages/mission-control/tests/security-txss.test.ts` exists
- [x] `packages/mission-control/tests/security-tcred.test.ts` exists
- [x] Commit 05af5717 exists (security-txss.test.ts + package.json)
- [x] Commit a4b8868e exists (security-tcred.test.ts)
- [x] Both files run without syntax errors: 4 pass, 8 fail (12 tests total)
- [x] RED baseline confirmed: B67, B68, B69, B70, B72, B73, B75, B76 all fail
- [x] GREEN confirmed: B65, B66, B71, B74 all pass
