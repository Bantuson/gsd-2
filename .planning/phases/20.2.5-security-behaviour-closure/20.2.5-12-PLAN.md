---
phase: 20.2.5-security-behaviour-closure
plan: "12"
type: execute
wave: 6
depends_on: ["20.2.5-05", "20.2.5-06", "20.2.5-07", "20.2.5-08", "20.2.5-09", "20.2.5-10", "20.2.5-11"]
autonomous: true
requirements: [SEC-ALL]
files_modified:
  - packages/mission-control/tests/security-tfile.test.ts
  - packages/mission-control/tests/security-texec.test.ts
  - packages/mission-control/tests/security-tnet.test.ts
  - packages/mission-control/tests/security-tauth.test.ts
  - packages/mission-control/tests/security-txss.test.ts
  - packages/mission-control/tests/security-tcred.test.ts

must_haves:
  truths:
    - "All 6 security test files run GREEN — no failing tests"
    - "The count of passing behaviour assertions across all 6 files is at least 53 (covering all FAIL behaviours from the audit, minus 3 explicitly out-of-scope: B60, B61, B77)"
    - "Existing non-security test suite has no new failures compared to pre-phase-20.2.5 baseline"
    - "Rust compilation succeeds (cargo build in src-tauri)"
    - "The Bun server starts on the configured port without errors after all changes"
  artifacts:
    - path: packages/mission-control/tests/security-tfile.test.ts
      provides: "All B1-B18 tests GREEN"
    - path: packages/mission-control/tests/security-texec.test.ts
      provides: "All B25-B32 tests GREEN"
    - path: packages/mission-control/tests/security-tnet.test.ts
      provides: "All B33-B49 tests GREEN (B45-B47 marked todo with Rust test reference)"
    - path: packages/mission-control/tests/security-tauth.test.ts
      provides: "All B50-B56, B59, B63, B64 tests GREEN"
    - path: packages/mission-control/tests/security-txss.test.ts
      provides: "All B65-B70 tests GREEN"
    - path: packages/mission-control/tests/security-tcred.test.ts
      provides: "All B71-B76 tests GREEN"
---

<objective>
Run all 6 security test files in sequence, fix any remaining failures, and confirm no
regressions in the existing test suite. This is the final gate before phase 20.2.5 is complete.

Out-of-scope behaviours (excluded from GREEN requirement):
- B60, B61 (OAuth session binding) — deferred to phase 20.2.6
- B77 (CI signing key) — deferred to CI/CD phase

All other 78 in-scope behaviours must be GREEN.

Purpose: Verify all remediations are complete and the codebase is regression-free.
Output: GREEN security test suite, clean regression run, Rust compilation success.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.2.5-security-behaviour-closure/20.2.5-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all security test files and fix remaining failures</name>
  <files>packages/mission-control/tests/security-tfile.test.ts, packages/mission-control/tests/security-texec.test.ts, packages/mission-control/tests/security-tnet.test.ts, packages/mission-control/tests/security-tauth.test.ts, packages/mission-control/tests/security-txss.test.ts, packages/mission-control/tests/security-tcred.test.ts</files>
  <action>
Run all 6 security test files and capture results:
```
cd packages/mission-control && bun test tests/security-tfile.test.ts tests/security-texec.test.ts tests/security-tnet.test.ts tests/security-tauth.test.ts tests/security-txss.test.ts tests/security-tcred.test.ts 2>&1 | tee /tmp/security-test-results.txt
```

For each FAILING test:
1. Read the test name and failure message
2. Identify which source file contains the incomplete remediation
3. Fix the source file
4. Re-run the specific test to confirm it turns GREEN
5. Move to the next failure

Expected remaining failures by category:

**T-FILE:** If any B1-B18 tests still fail, check:
- validateSegment not imported/called in that specific API
- validatePath not called after segment validation
- Mode 0o600 not applied to Bun.write calls (use `{ mode: 0o600 }` option)

**T-EXEC (B25-B32):** If CSP tests fail, check tauri.conf.json was saved correctly.
If DOMPurify tests fail, check sanitize-html.ts is imported correctly.

**T-NET (B39):** The proxy CSP-stripping test — if still RED, check proxy-api.ts
`IFRAME_STRIP_HEADERS` does not include 'content-security-policy'. Fix if needed.

**T-AUTH (B50-B56):** If unauthenticated request tests fail, check:
- Token middleware is placed correctly in server.ts (after OPTIONS handler, before routes)
- `/api/auth/startup-token` is excluded from token validation
- WS token check is reading from the correct query parameter or header

**T-XSS (B67):** If CSP wildcard test fails, check tauri.conf.json connect-src section.

**T-CRED (B73):** If trust fail-closed test fails, check App.tsx catch block.

For any test that covers B60, B61, or B77, mark as `it.skip("B60: deferred to phase 20.2.6")`.

Remove the `// RED PHASE:` comments from test file headers and replace with:
```
// Phase 20.2.5 Security Behaviour Tests
// All tests in this file MUST be GREEN.
// Exceptions: B60, B61 (OAuth nonce binding — deferred to 20.2.6), B77 (CI signing key — deferred)
```
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test tests/security-tfile.test.ts tests/security-texec.test.ts tests/security-tnet.test.ts tests/security-tauth.test.ts tests/security-txss.test.ts tests/security-tcred.test.ts 2>&1 | grep -E "^✓|^✗|passed|failed" | tail -20</automated>
  </verify>
  <done>All 6 security test files pass with 0 failures (excluding it.skip for B60, B61, B77). Total passing behaviour assertions ≥ 53.</done>
</task>

<task type="auto">
  <name>Task 2: Run regression suite and Rust compilation check</name>
  <files>packages/mission-control/tests/security-tfile.test.ts</files>
  <action>
**Step 1 — Full existing test suite regression check:**
```
cd packages/mission-control && bun test --exclude "tests/security-*" 2>&1 | tail -30
```

For any newly-broken tests:
- If the test calls an API endpoint directly (without auth token): update the test to
  include the launch token. Import `getTestToken` from security-test-helpers.ts.
- If the test imports a module that now has different exports: update the import.
- If the test relies on `Math.random()` for IDs: update to accept UUID format.

Common regression patterns to watch for:
1. Tests that call `/api/*` without token → add `Authorization: Bearer` header
2. Tests checking error message text → error messages changed to generic form
3. Tests checking CSP string → CSP has changed in tauri.conf.json
4. Tests checking auth.json structure → may have changed

Fix all regressions. Do NOT weaken the security changes to fix regressions — update the tests
to work with the new security posture.

**Step 2 — Rust compilation:**
```
cd packages/mission-control/src-tauri && cargo build 2>&1 | tail -10
```

If cargo build fails, read the error output and fix the Rust code.

**Step 3 — Server startup smoke test:**
```
cd packages/mission-control && timeout 10 bun run src/server.ts 2>&1 | head -20
```
The server should start and print "Listening on http://127.0.0.1:4200" without crashing.
The per-launch token should be generated (check for any startup log).
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test --exclude "tests/security-*" 2>&1 | grep -E "passed|failed|error" | tail -5</automated>
  </verify>
  <done>Full regression suite passes. 0 new test failures introduced by Phase 20.2.5 changes. Rust compiles. Server starts cleanly.</done>
</task>

</tasks>

<verification>
Final summary verification:

```bash
# All security tests
cd packages/mission-control && bun test tests/security-*.test.ts 2>&1 | grep -E "passed|failed"

# Regression suite
cd packages/mission-control && bun test --exclude "tests/security-*" 2>&1 | grep -E "passed|failed"

# Rust build
cd packages/mission-control/src-tauri && cargo build 2>&1 | grep -E "error|warning|Finished"
```

Expected output:
- Security tests: "N passed, 0 failed" (N ≥ 53 in-scope behaviours)
- Regression suite: same pass count as before phase 20.2.5, 0 new failures
- Rust: "Finished" with no errors
</verification>

<success_criteria>
- All 6 security test files GREEN (0 failures, excluding explicitly skipped B60/B61/B77)
- Existing test suite: 0 new failures
- Rust: cargo build completes without errors
- Server starts without errors
- Phase 20.2.5 complete: 53 FAIL + 11 PARTIAL behaviours from audit now PASS
</success_criteria>

<output>
After completion, create `.planning/phases/20.2.5-security-behaviour-closure/20.2.5-12-SUMMARY.md`
</output>
