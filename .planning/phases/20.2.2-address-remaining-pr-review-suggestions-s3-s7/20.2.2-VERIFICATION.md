---
phase: 20.2.2-address-remaining-pr-review-suggestions-s3-s7
verified: 2026-03-19T00:00:00Z
status: passed
score: 5/5 must-haves verified
re_verification: false
---

# Phase 20.2.2: Address Remaining PR Review Suggestions (S3-S7) Verification Report

**Phase Goal:** All 5 remaining PR review suggestions from @frizynn are resolved — %2E%2E path traversal test, merge action UI fix, WS auth comment, releaseDraft config, window label race condition.
**Verified:** 2026-03-19
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `fs-api.ts` has a test case for `%2E%2E` URL-encoded path traversal | VERIFIED | `fs-api.test.ts` line 129: test calls `decodeURIComponent("%2E%2E%2F..%2Fetc%2Fpasswd")` and asserts `validatePath` throws |
| 2 | `session-manager.ts` merge action returns an error to the client | VERIFIED | Line 195: `throw new Error("Merge not yet implemented — use discard or keep")`; pipeline.ts catches and sends `session_error` |
| 3 | `ws-server.ts` has a comment documenting the intentional localhost-only WS auth decision | VERIFIED | Lines 70-81: `SECURITY NOTE` block comment above `fetch(req, server)` handler documenting 127.0.0.1-only binding and accepted risk |
| 4 | `release.yml` has `releaseDraft: false` | VERIFIED | Line 107: `releaseDraft: false`; zero matches for `releaseDraft: true` |
| 5 | `commands.rs` `open_new_window` uses a counter for window labels (not wall-clock millis) | VERIFIED | `AtomicU64`-based `WindowCounter` struct at lines 1-17; `open_new_window` accepts `tauri::State<'_, WindowCounter>` at line 169; zero matches for `SystemTime` |

**Score: 5/5 truths verified**

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/mission-control/tests/fs-api.test.ts` | Test case for `%2E%2E` URL-encoded path traversal | VERIFIED | Lines 129-135: test exists, calls `decodeURIComponent`, asserts `validatePath` throws with correct message |
| `packages/mission-control/src/server/session-manager.ts` | Merge action error return | VERIFIED | Lines 191-195: `case "merge"` throws `"Merge not yet implemented — use discard or keep"` with no fallthrough to delete |
| `packages/mission-control/src/server/ws-server.ts` | Localhost-only WS auth comment | VERIFIED | Lines 70-81: full `SECURITY NOTE` block present between `hostname: "127.0.0.1"` and `fetch` handler |
| `packages/mission-control/src/server/pipeline.ts` | `session_error` sent to client on merge throw | VERIFIED | Lines 325-342: `try-catch` wraps `closeSessionWithWorktree`; catch sends `{ type: "session_error", error: err.message, sessionId }` |
| `.github/workflows/release.yml` | `releaseDraft: false` | VERIFIED | Line 107: `releaseDraft: false`; confirmed zero occurrences of `releaseDraft: true` |
| `packages/mission-control/src-tauri/src/commands.rs` | `AtomicU64` window counter | VERIFIED | Lines 1-17: `WindowCounter(AtomicU64)` struct; `open_new_window` uses `counter.next()`; `SystemTime` fully removed |
| `packages/mission-control/src-tauri/src/lib.rs` | `WindowCounter` registered in Builder chain | VERIFIED | Line 17: `.manage(commands::WindowCounter::new())` present after `.manage(bun_state)` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `packages/mission-control/tests/fs-api.test.ts` | `packages/mission-control/src/server/fs-api.ts` | `import { validatePath }` | WIRED | Line 5: `import { listDirectory, detectProject, validatePath } from "../src/server/fs-api"` — test calls `validatePath(decoded)` directly |
| `packages/mission-control/src/server/pipeline.ts` | `session-manager.ts` | `closeSessionWithWorktree` wrapped in try-catch | WIRED | Lines 325-342: call site wraps `closeSessionWithWorktree` in try-catch; catch block sends `session_error` via `wsServer.sendToClient` |
| `packages/mission-control/src-tauri/src/commands.rs` | `packages/mission-control/src-tauri/src/lib.rs` | `commands::open_new_window` in `invoke_handler` | WIRED | `lib.rs` line 83: `commands::open_new_window` registered in `invoke_handler`; `WindowCounter` managed at line 17 |

---

### Requirements Coverage

The requirement IDs S3-S7 are PR review suggestion labels, not entries in `REQUIREMENTS.md` (which uses COMPAT-*, STREAM-*, TAURI-*, etc. for v2.0 requirements). This is consistent with the phase being a late-stage PR review feedback cycle not anticipated in the original requirements specification.

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| S3 | Plan 01 | `%2E%2E` URL-encoded path traversal test | SATISFIED | Test in `fs-api.test.ts` line 129; `decodeURIComponent` + `validatePath` assertion |
| S4 | Plan 01 | Merge action error instead of silent delete | SATISFIED | `session-manager.ts` line 195 throws; `pipeline.ts` sends `session_error` |
| S5 | Plan 01 | WS auth documentation comment | SATISFIED | `ws-server.ts` lines 70-81 `SECURITY NOTE` block |
| S6 | Plan 02 | `releaseDraft: false` for OTA compatibility | SATISFIED | `release.yml` line 107 |
| S7 | Plan 02 | Atomic counter for window labels | SATISFIED | `commands.rs` `WindowCounter(AtomicU64)` + `lib.rs` `.manage()` registration |

**Orphaned requirements:** None. All S3-S7 identifiers appear in plan frontmatter and are verified in the codebase. REQUIREMENTS.md does not define S3-S7 — these are external PR review tracking IDs, not v2.0 spec requirements.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `packages/mission-control/src/server/session-manager.ts` | 174 | `TODO — merge branch into main (complex, future work)` | Info | JSDoc comment in method signature describing merge as future work — appropriate, consistent with the throw at line 195 |
| `packages/mission-control/src/server/pipeline.ts` | 91 | `TODO (Phase 13): read skip_permissions from...` | Info | Pre-existing, unrelated to this phase |

No blockers. No warnings. Both TODO items are informational documentation notes, not code stubs.

---

### Commit Verification

All commits documented in SUMMARYs confirmed present in git history:

| Commit | Message |
|--------|---------|
| `92afff5` | test(20.2.2-01): add %2E%2E URL-encoded path traversal test (S3) |
| `e9a8c92` | fix(20.2.2-01): merge action throws error instead of silently deleting (S4) |
| `c6514d5` | docs(20.2.2-01): add SECURITY NOTE comment for localhost-only WS auth decision (S5) |
| `6753259` | fix(20.2.2-02): set releaseDraft: false so OTA auto-updates work without manual publish (S6) |
| `2e7e428` | fix(20.2.2-02): replace wall-clock window label with AtomicU64 counter (S7) |
| `700b06c` | fix(20.2.2-02): update release-workflow test to assert releaseDraft: false |

---

### Human Verification Required

None required. All changes are code-level (not UI/UX/visual/real-time behavior) and have been fully verified programmatically.

---

## Summary

Phase 20.2.2 goal is fully achieved. All five PR review suggestions from @frizynn are addressed and verified in the codebase:

- **S3**: `fs-api.test.ts` has the `%2E%2E` test documenting Bun's URL-decode behavior feeding `validatePath`.
- **S4**: `session-manager.ts` merge case throws an error; `pipeline.ts` catches and sends a typed `session_error` message to the client — no silent delete.
- **S5**: `ws-server.ts` has a complete `SECURITY NOTE` block co-located with the `127.0.0.1` hostname binding, documenting the intentional localhost-only auth decision.
- **S6**: `release.yml` line 107 reads `releaseDraft: false` — OTA auto-update endpoint unblocked.
- **S7**: `commands.rs` defines `WindowCounter(AtomicU64)` registered in `lib.rs` via `.manage()`; `open_new_window` uses `counter.next()` — wall-clock `SystemTime` fully removed.

No regressions introduced. No stubs or orphaned artifacts found.

---

_Verified: 2026-03-19_
_Verifier: Claude (gsd-verifier)_
