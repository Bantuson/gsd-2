---
phase: 20.2.2-address-remaining-pr-review-suggestions-s3-s7
plan: 01
subsystem: testing, security, api
tags: [path-traversal, websocket, session-manager, security, bun]

requires:
  - phase: 20.2.1-fix-pr-blocker-issues
    provides: CI green baseline with 1042 passing tests

provides:
  - "%2E%2E URL-encoded path traversal test documenting Bun decode behavior"
  - "Merge action throws error instead of silently deleting worktree"
  - "pipeline.ts session_close sends session_error to client on merge attempt"
  - "ws-server.ts SECURITY NOTE comment documenting localhost-only WS auth decision"

affects:
  - 20.2.2 (plans 02+) — remaining PR suggestions S6, S7
  - PR reviewer @frizynn — S3, S4, S5 addressed

tech-stack:
  added: []
  patterns:
    - "decodeURIComponent in test to simulate Bun URL param decoding before handler"
    - "throw Error in unimplemented case rather than silent fallthrough"
    - "try-catch in pipeline session_close dispatching session_error to client"

key-files:
  created: []
  modified:
    - packages/mission-control/tests/fs-api.test.ts
    - packages/mission-control/src/server/session-manager.ts
    - packages/mission-control/src/server/pipeline.ts
    - packages/mission-control/src/server/ws-server.ts

key-decisions:
  - "Merge case throws 'Merge not yet implemented — use discard or keep' — no fallthrough to delete; session stays intact"
  - "pipeline.ts session_close handler uses try-catch to send session_error type to client on merge attempt"
  - "ws-server SECURITY NOTE placed between hostname binding and fetch handler — co-located with the design decision it documents"

patterns-established:
  - "Unimplemented actions throw errors with clear message rather than silently degrading"
  - "Pipeline action handlers catch errors from session-manager and relay them as typed error messages to the client"

requirements-completed: [S3, S4, S5]

duration: 12min
completed: 2026-03-19
---

# Phase 20.2.2 Plan 01: Address PR Review Suggestions S3, S4, S5 Summary

**%2E%2E traversal test, merge-action error throw, and WS auth SECURITY NOTE comment — three of five PR review suggestions closed**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-03-19T00:00:00Z
- **Completed:** 2026-03-19T00:12:00Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments
- Added test case in fs-api.test.ts documenting that Bun decodes %2E%2E to ".." before the handler, and that the existing validatePath check catches it
- Fixed session-manager merge case: now throws "Merge not yet implemented — use discard or keep" instead of silently deleting the worktree
- Added try-catch in pipeline.ts session_close handler that sends a typed session_error message to the client when merge fails
- Added SECURITY NOTE block comment above the WebSocket upgrade fetch handler in ws-server.ts explaining the intentional localhost-only auth decision

## Task Commits

Each task was committed atomically:

1. **Task 1: Add %2E%2E URL-encoded path traversal test (S3)** - `92afff5` (test)
2. **Task 2: Fix merge action silent delete in session-manager.ts (S4)** - `e9a8c92` (fix)
3. **Task 3: Add localhost-only WS auth documentation comment (S5)** - `c6514d5` (docs)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `packages/mission-control/tests/fs-api.test.ts` - Added %2E%2E decodeURIComponent test case inside validatePath describe block
- `packages/mission-control/src/server/session-manager.ts` - Merge case now throws instead of silently deleting
- `packages/mission-control/src/server/pipeline.ts` - session_close handler wrapped in try-catch; sends session_error on merge throw
- `packages/mission-control/src/server/ws-server.ts` - SECURITY NOTE block comment added above fetch handler

## Decisions Made
- Merge case throws rather than returning — callers should not silently absorb an unimplemented action that has destructive side effects; throw forces explicit error handling
- session_error message type sent to client uses `err.message` with a fallback — consistent with other error relay patterns in pipeline.ts
- SECURITY NOTE placed between the `hostname: "127.0.0.1"` line and `fetch(req, server)` — the binding decision and the auth decision are documented together at the point of implementation

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- 5 pre-existing test failures confirmed unchanged before and after all task commits (verified via `git stash`). Failures are in release-workflow and Session WebSocket Routing tests and are unrelated to this plan's changes.

## Next Phase Readiness
- S3, S4, S5 fully addressed and committed
- Ready to execute plan 02 addressing remaining PR suggestions S6 and S7
- Full test suite: 1038 pass, 5 pre-existing failures, 3 todo (no regressions introduced)

---
*Phase: 20.2.2-address-remaining-pr-review-suggestions-s3-s7*
*Completed: 2026-03-19*
