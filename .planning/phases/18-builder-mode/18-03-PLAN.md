---
phase: 18-builder-mode
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - packages/mission-control/src/components/milestone/SlicePlanned.tsx
  - packages/mission-control/src/components/milestone/SliceInProgress.tsx
  - packages/mission-control/src/components/milestone/SliceNeedsReview.tsx
  - packages/mission-control/src/components/milestone/SliceComplete.tsx
  - packages/mission-control/src/components/milestone/SliceRow.tsx
  - packages/mission-control/src/components/milestone/SliceAccordion.tsx
  - packages/mission-control/src/components/chat/QuestionCard.tsx
  - packages/mission-control/src/components/chat/DecisionLogDrawer.tsx
autonomous: true
requirements:
  - BUILDER-05
  - BUILDER-06

must_haves:
  truths:
    - "In Builder mode, SlicePlanned shows 'Ready to build' state label and 'See what will be built' / 'Build this feature' action labels"
    - "In Builder mode, SliceInProgress shows 'Building now' state label and 'Give direction' action label"
    - "In Builder mode, SliceNeedsReview shows 'Ready for your review' state label and 'Ship it' action label instead of 'Merge to main'"
    - "In Builder mode, SliceComplete shows 'Done' state label"
    - "StatusBadge in SliceRow shows Builder-mode labels when builderMode=true"
    - "QuestionCard in Builder mode hides the 'area' label (GSD terminology) or replaces it with a plain-language equivalent"
    - "DecisionLogDrawer header shows 'Your decisions so far' instead of 'Decisions' in Builder mode"
    - "Developer mode behaviour is 100% unchanged — no regressions in any existing slice card test"
  artifacts:
    - path: "packages/mission-control/src/components/milestone/SlicePlanned.tsx"
      provides: "SlicePlanned with builderMode prop"
      exports: ["SlicePlanned"]
    - path: "packages/mission-control/src/components/milestone/SliceInProgress.tsx"
      provides: "SliceInProgress with builderMode prop"
      exports: ["SliceInProgress"]
    - path: "packages/mission-control/src/components/milestone/SliceNeedsReview.tsx"
      provides: "SliceNeedsReview with builderMode prop"
      exports: ["SliceNeedsReview"]
    - path: "packages/mission-control/src/components/milestone/SliceComplete.tsx"
      provides: "SliceComplete with builderMode prop"
      exports: ["SliceComplete"]
    - path: "packages/mission-control/src/components/milestone/SliceRow.tsx"
      provides: "SliceRow and StatusBadge with builderMode prop"
    - path: "packages/mission-control/src/components/chat/QuestionCard.tsx"
      provides: "QuestionCard with builderMode prop hiding area label"
    - path: "packages/mission-control/src/components/chat/DecisionLogDrawer.tsx"
      provides: "DecisionLogDrawer with builderMode prop for header"
  key_links:
    - from: "packages/mission-control/src/components/milestone/SliceRow.tsx"
      to: "packages/mission-control/src/components/milestone/SlicePlanned.tsx"
      via: "builderMode prop threaded from SliceRow to each slice card"
      pattern: "builderMode"
    - from: "packages/mission-control/src/components/milestone/SliceAccordion.tsx"
      to: "packages/mission-control/src/components/milestone/SliceRow.tsx"
      via: "builderMode prop passed from SliceAccordion which reads useBuilderMode()"
      pattern: "useBuilderMode"
---

<objective>
Apply Builder vocabulary to all four slice card components and the discuss-mode components (QuestionCard, DecisionLogDrawer). All changes are prop-conditional — no components duplicated.

Purpose: Completes the visual Builder mode experience. With Plan 18-01 (context + settings) and Plan 18-02 (classifier), this plan delivers the relabeled UI surfaces that non-technical users actually see.
Output: builderMode prop on SlicePlanned, SliceInProgress, SliceNeedsReview, SliceComplete, SliceRow, QuestionCard, DecisionLogDrawer. SliceAccordion reads useBuilderMode() and threads it down.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-builder-mode/18-RESEARCH.md
@.planning/phases/18-builder-mode/18-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase. No exploration needed. -->

From packages/mission-control/src/components/milestone/SlicePlanned.tsx (current):
```typescript
interface SlicePlannedProps {
  slice: GSD2SliceInfo;
  onAction: (action: SliceAction) => void;
  // ADD: builderMode?: boolean
}
// Current strings to replace with conditional:
// Line ~20: "PLANNED" → builderMode ? 'Ready to build' : 'PLANNED'
// Line ~57: "Review plan" → builderMode ? 'See what will be built' : 'Review plan'
// Line ~74: "Start this slice" → builderMode ? 'Build this feature' : 'Start this slice'
```

From packages/mission-control/src/components/milestone/SliceInProgress.tsx (pattern — read the file):
```typescript
// Current strings to make conditional:
// State label ("● EXECUTING") → builderMode ? 'Building now' : '● EXECUTING'
// Steer button label → builderMode ? 'Give direction' : 'Steer'
// Any "View task" / "Pause" labels remain in Developer mode only
// ADD: builderMode?: boolean prop
```

From packages/mission-control/src/components/milestone/SliceNeedsReview.tsx (pattern — read the file):
```typescript
// Current strings:
// State label ("⚠ NEEDS YOUR REVIEW") → builderMode ? 'Ready for your review' : '⚠ NEEDS YOUR REVIEW'
// "Merge to main" button → builderMode ? 'Ship it' : 'Merge to main'
// ADD: builderMode?: boolean prop
```

From packages/mission-control/src/components/milestone/SliceComplete.tsx (pattern — read the file):
```typescript
// Current strings:
// State label ("✓ COMPLETE") → builderMode ? 'Done' : '✓ COMPLETE'
// ADD: builderMode?: boolean prop
```

From packages/mission-control/src/components/milestone/SliceRow.tsx:
```typescript
interface SliceRowProps {
  // ...existing props...
  // ADD: builderMode?: boolean
}
// StatusBadge function needs builderMode prop:
function StatusBadge({ status, builderMode }: { status: SliceStatus, builderMode?: boolean })
// Builder status labels:
// planned → 'Ready to build'
// in_progress → 'Building now'
// needs_review → 'Ready for your review'
// complete → 'Done'
// SliceRow passes builderMode to all four slice card components
```

From packages/mission-control/src/components/milestone/SliceAccordion.tsx (read the file to see current structure):
```typescript
// SliceAccordion renders SliceRow for each slice
// Add: const { builderMode } = useBuilderMode()  (import useBuilderMode hook)
// Pass: builderMode={builderMode} to each <SliceRow>
```

From packages/mission-control/src/components/chat/QuestionCard.tsx:
```typescript
// QuestionCardViewProps — ADD: builderMode?: boolean
// QuestionCard — ADD: builderMode?: boolean
// Line 37-39: question.area label
// <span className="text-xs font-display uppercase tracking-wider text-cyan-accent">
//   {question.area}
// </span>
// In Builder mode: hide this span entirely (return null) OR replace with a plain generic label
// Simplest per research: hide it (area is GSD-internal; "Question N of N" already shows progress)
// builderMode ? null : <span ...>{question.area}</span>
```

From packages/mission-control/src/components/chat/DecisionLogDrawer.tsx:
```typescript
// DecisionLogDrawerProps — ADD: builderMode?: boolean
// Line 19-21:
// <span className="text-xs font-display uppercase tracking-wider text-slate-400">
//   Decisions
// </span>
// Change to: builderMode ? 'Your decisions so far' : 'Decisions'
```

From packages/mission-control/src/hooks/useChatMode.tsx (to find where DecisionLogDrawer is used):
// DecisionLogDrawer is rendered inside useChatMode or ChatView — read useChatMode.tsx to find
// the call site and add builderMode prop threading there
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Builder labels on all four slice card components + SliceRow StatusBadge</name>
  <files>
    packages/mission-control/src/components/milestone/SlicePlanned.tsx,
    packages/mission-control/src/components/milestone/SliceInProgress.tsx,
    packages/mission-control/src/components/milestone/SliceNeedsReview.tsx,
    packages/mission-control/src/components/milestone/SliceComplete.tsx,
    packages/mission-control/src/components/milestone/SliceRow.tsx,
    packages/mission-control/src/components/milestone/SliceAccordion.tsx
  </files>
  <action>
CRITICAL: Read each file with the Read tool before modifying. Do not guess current content.

Label map to apply (do NOT create a separate object — inline ternaries are fine for 1-2 labels per component):

SlicePlanned:
  - Add `builderMode?: boolean` to `SlicePlannedProps`
  - State label: `{builderMode ? 'Ready to build' : 'PLANNED'}`
  - "Review plan" button: `{builderMode ? 'See what will be built' : 'Review plan'}`
  - "Start this slice" button: `{builderMode ? 'Build this feature' : 'Start this slice'}`
  - No other changes

SliceInProgress:
  - Read the file first to identify all hardcoded strings
  - Add `builderMode?: boolean` to its props interface
  - State label: `{builderMode ? 'Building now' : '● EXECUTING'}`
  - Steer button: `{builderMode ? 'Give direction' : 'Steer'}`
  - Other labels (View task, etc.): leave as-is (only the spec-listed labels change)

SliceNeedsReview:
  - Read the file first
  - Add `builderMode?: boolean` to its props interface
  - State label: `{builderMode ? 'Ready for your review' : '⚠ NEEDS YOUR REVIEW'}`
  - "Merge to main" button: `{builderMode ? 'Ship it' : 'Merge to main'}`
  - UAT checklist labels: leave as-is (checkboxes are functional, not GSD terminology)

SliceComplete:
  - Read the file first
  - Add `builderMode?: boolean` to its props interface
  - State label: `{builderMode ? 'Done' : '✓ COMPLETE'}`
  - Other view links: leave as-is (View diff, View UAT results are generic enough)

SliceRow:
  - Add `builderMode?: boolean` to `SliceRowProps`
  - Update `StatusBadge` function signature: `{ status: SliceStatus, builderMode?: boolean }`
  - In each StatusBadge case, use builderMode to substitute text:
    - planned: `builderMode ? 'Ready to build' : 'PLANNED'`
    - in_progress: `builderMode ? 'Building now' : '● EXECUTING'`
    - needs_review: `builderMode ? 'Ready for your review' : '⚠ NEEDS YOUR REVIEW'`
    - complete: `builderMode ? 'Done' : '✓ COMPLETE'`
  - Pass `builderMode={builderMode}` to StatusBadge call
  - Pass `builderMode={builderMode}` to each slice card: SlicePlanned, SliceInProgress, SliceNeedsReview, SliceComplete

SliceAccordion:
  - Read the file to see current structure
  - Add import: `import { useBuilderMode } from '@/hooks/useBuilderMode'`
  - Add inside component body: `const { builderMode } = useBuilderMode()`
  - Pass `builderMode={builderMode}` to each `<SliceRow>` rendered

ANTI-PATTERN TO AVOID: Do not create BuilderSlicePlanned or any duplicate component. All changes are additive prop-conditional within existing components.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test 2>&1 | tail -30</automated>
  </verify>
  <done>Full test suite passes (700+ tests, no regressions). All four slice card files have builderMode prop. SliceRow StatusBadge uses Builder labels when builderMode=true. SliceAccordion reads useBuilderMode() and passes builderMode to SliceRow.</done>
</task>

<task type="auto">
  <name>Task 2: Builder vocabulary in QuestionCard and DecisionLogDrawer</name>
  <files>
    packages/mission-control/src/components/chat/QuestionCard.tsx,
    packages/mission-control/src/components/chat/DecisionLogDrawer.tsx,
    packages/mission-control/src/hooks/useChatMode.tsx
  </files>
  <action>
CRITICAL: Read each file with the Read tool before modifying.

1. **QuestionCard.tsx**:
   - Add `builderMode?: boolean` to `QuestionCardViewProps` interface
   - Add `builderMode?: boolean` to `QuestionCard` props interface (the stateful wrapper)
   - In `QuestionCardView`: at the area label span (currently `{question.area}`):
     - When `builderMode === true`: hide the area label entirely — render `null` in place of the span
     - When `builderMode === false` (or undefined): show `{question.area}` as before
     - The "Question N of N" progress display (already implemented) stays unchanged — it is already correct for Builder mode
   - Pass `builderMode={builderMode}` from `QuestionCard` to `QuestionCardView`

2. **DecisionLogDrawer.tsx**:
   - Add `builderMode?: boolean` to `DecisionLogDrawerProps`
   - Change the header span text: `{builderMode ? 'Your decisions so far' : 'Decisions'}`

3. **useChatMode.tsx** (or wherever DecisionLogDrawer and QuestionCard are rendered):
   - Read the file to find the render call sites
   - Import `useBuilderMode` hook
   - Get `const { builderMode } = useBuilderMode()`
   - Pass `builderMode={builderMode}` to DecisionLogDrawer and QuestionCard render calls

   Note: QuestionCard may also be rendered in ChatPanel.tsx or SingleColumnView.tsx — check useChatMode.tsx first, then grep for QuestionCard usage. Pass builderMode through whichever component renders it.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test 2>&1 | tail -30</automated>
  </verify>
  <done>Full test suite 700+ tests green. QuestionCard hides area label in Builder mode. DecisionLogDrawer shows 'Your decisions so far' in Builder mode. useChatMode passes builderMode to both components.</done>
</task>

</tasks>

<verification>
After both tasks:
`cd packages/mission-control && bun test 2>&1 | tail -30`
All existing slice card tests and question card tests must remain green. No new test files needed — the existing tests exercise Developer mode paths which must be unchanged.

Spot-check source text:
- SlicePlanned.tsx contains 'Build this feature'
- SliceRow.tsx contains 'Ready to build'
- DecisionLogDrawer.tsx contains 'Your decisions so far'
- QuestionCard.tsx has conditional on builderMode for area span
</verification>

<success_criteria>
- SlicePlanned: builderMode=true shows "Ready to build", "See what will be built", "Build this feature"
- SliceInProgress: builderMode=true shows "Building now", "Give direction"
- SliceNeedsReview: builderMode=true shows "Ready for your review", "Ship it"
- SliceComplete: builderMode=true shows "Done"
- StatusBadge in SliceRow uses Builder labels when builderMode=true
- SliceAccordion reads useBuilderMode() and passes builderMode down
- QuestionCard in Builder mode hides the area label
- DecisionLogDrawer in Builder mode shows "Your decisions so far"
- Full bun test suite: no regressions (700+ tests)
</success_criteria>

<output>
After completion, create `.planning/phases/18-builder-mode/18-03-SUMMARY.md`
</output>
