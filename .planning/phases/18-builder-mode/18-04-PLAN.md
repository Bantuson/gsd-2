---
phase: 18-builder-mode
plan: 04
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
  - 18-03
files_modified: []
autonomous: false
requirements:
  - BUILDER-01
  - BUILDER-02
  - BUILDER-03
  - BUILDER-04
  - BUILDER-05
  - BUILDER-06
  - BUILDER-07

must_haves:
  truths:
    - "Switching to Builder mode in Settings immediately relabels the UI without restarting the session"
    - "Builder mode chat input shows the correct placeholder and no slash autocomplete"
    - "Natural language messages in Builder mode route through the classifier and show a routing badge"
    - "Discuss cards in Builder mode show plain-language labels and no GSD area terminology"
    - "Slice cards in Builder mode show all five Builder state and action labels correctly"
    - "Full bun test suite passes with no regressions"
  artifacts:
    - path: "packages/mission-control/src/context/InterfaceModeContext.tsx"
      provides: "Context in production"
    - path: "packages/mission-control/src/server/classify-intent-api.ts"
      provides: "Classifier in production"
  key_links:
    - from: "Human verification"
      to: "SC-1 through SC-5"
      via: "Manual walkthrough of 5 success criteria"
      pattern: "n/a"
---

<objective>
Human verification of all 7 BUILDER requirements across 5 success criteria. Run the full automated test suite first to confirm no regressions, then walk through each success criterion manually in the running app.

Purpose: Builder mode is a user-facing mode change — visual correctness requires a human eye. The automated tests cover logic; this plan confirms the live UI experience.
Output: Human approval of SC-1 through SC-5, Phase 18 marked complete.
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-builder-mode/18-01-SUMMARY.md
@.planning/phases/18-builder-mode/18-02-SUMMARY.md
@.planning/phases/18-builder-mode/18-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated test suite gate</name>
  <files></files>
  <action>
Run the complete test suite. It must pass with no regressions before human verification begins.

Commands to run in sequence:
1. `cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test 2>&1 | tail -30`
2. `cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test tests/builder-mode.test.ts --timeout 15000 2>&1`
3. `cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun test tests/classify-intent.test.ts --timeout 15000 2>&1`

If any test fails:
- Identify the failing test and root cause
- Fix the issue in the appropriate plan's files
- Re-run until all tests pass
- Do NOT proceed to the human verification checkpoint until the full suite is green

Expected: 700+ tests passing, 0 failures, 0 regressions from Plans 18-01 through 18-03.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun test 2>&1 | tail -10</automated>
  </verify>
  <done>Full suite green. Output shows "X tests passed" with no failures. builder-mode.test.ts and classify-intent.test.ts all GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Builder mode: a complete vocabulary and routing layer on top of Developer mode.

Plans 18-01 through 18-03 delivered:
- InterfaceModeContext propagating builderMode to all components via useBuilderMode()
- Settings toggle "Interface Mode" persisting as interface_mode in preferences.md
- ChatInput with Builder placeholder, hidden slash autocomplete, no command palette
- POST /api/classify-intent server route with Claude Haiku routing
- RoutingBadge showing how each message was classified (with Override)
- PhaseGateCard intercept for UI_PHASE_GATE intent
- All 4 slice cards with Builder state/action labels (SlicePlanned, SliceInProgress, SliceNeedsReview, SliceComplete)
- QuestionCard with hidden area label in Builder mode
- DecisionLogDrawer with "Your decisions so far" header in Builder mode
  </what-built>
  <how-to-verify>
Start the app: `cd /c/Users/Bantu/mzansi-agentive/gsd-2/packages/mission-control && bun dev`
Then open http://localhost:4000

**SC-1: Mode toggle relabels UI immediately (BUILDER-01, BUILDER-02)**
1. Click Settings (gear icon in sidebar)
2. Find "Interface Mode" section — should show Developer / Builder options
3. Switch to Builder mode
4. Navigate to Milestones view
5. VERIFY: Slice cards show "Feature" not "Slice" terminology; state labels show "Ready to build", "Building now", etc.
6. Return to Settings, switch back to Developer mode
7. VERIFY: Labels return to "PLANNED", "● EXECUTING", etc.
8. VERIFY: No session restart occurred — chat history still present

**SC-2: Builder chat input (BUILDER-03)**
1. Ensure Builder mode is active (switch in Settings)
2. Click on Chat view
3. VERIFY: Input placeholder shows "What do you want to build or change?"
4. Type "/" in the input
5. VERIFY: No slash command autocomplete dropdown appears
6. Press Ctrl+Shift+P (or Cmd+Shift+P on Mac)
7. VERIFY: Command palette does NOT open (or stays empty/closed)

**SC-3: Routing badge (BUILDER-04)**
1. Ensure Builder mode is active
2. Type a natural language message: "I want to build a landing page" and press Enter
3. WAIT up to 3 seconds for routing to complete
4. VERIFY: A routing badge appears showing how the message was classified (e.g. "Sent as: Coding task")
5. VERIFY: An "Override" button is visible in the badge
6. Click Override
7. VERIFY: Badge dismisses and message returns to normal flow

**SC-4: Discuss cards (BUILDER-05)**
Note: This requires an active GSD discuss session. If no discuss session is running, verify via static inspection:
- In the running app, check if the QuestionCard area label ("Constraint", "Architecture", etc.) is hidden in Builder mode
- If a discuss session is available: start it, confirm "Question N of N" shows, confirm area label is not shown

**SC-5: Slice cards Builder labels (BUILDER-06)**
1. Navigate to Milestones view with Builder mode active
2. VERIFY for each slice state visible:
   - Planned slices show: "Ready to build" state label, "See what will be built" / "Build this feature" buttons
   - In-progress slices show: "Building now" state, "Give direction" steer button
   - Needs-review slices show: "Ready for your review" state, "Ship it" merge button
   - Complete slices show: "Done" state label
3. Switch to Developer mode
4. VERIFY all labels revert to developer terminology
  </how-to-verify>
  <resume-signal>
Type "approved" if all 5 success criteria pass.

If any criterion fails, describe the issue (e.g. "SC-2 failed: slash autocomplete still showing") and Claude will fix and re-present.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 18 is complete when:
1. Full bun test suite passes (700+ tests, 0 failures)
2. Human approval received: SC-1, SC-2, SC-3, SC-4, SC-5 all verified

All 7 BUILDER requirements addressed:
- BUILDER-01: Settings toggle + InterfaceModeContext propagation (Plan 18-01)
- BUILDER-02: Vocabulary map (Plan 18-01, 18-03)
- BUILDER-03: Chat input changes (Plan 18-01)
- BUILDER-04: Intent classifier API route + routing badge (Plan 18-02)
- BUILDER-05: Discuss cards (Plan 18-03)
- BUILDER-06: Slice cards (Plan 18-03)
- BUILDER-07: Phase gate card (Plan 18-02)
</verification>

<success_criteria>
- Human approves SC-1 through SC-5
- Full bun test suite: 700+ tests passing, 0 regressions
- Phase 18 marked complete in STATE.md and ROADMAP.md
</success_criteria>

<output>
After human approval, create `.planning/phases/18-builder-mode/18-04-SUMMARY.md`

Then update STATE.md:
- last_activity: "2026-03-14 — Phase 18 complete: Builder mode with InterfaceModeContext, intent classifier, Builder vocabulary on slice/discuss cards, human verification SC-1..SC-5 approved"
- stopped_at: "Completed 18-04-PLAN.md — Phase 18 Builder Mode fully complete"
- progress.completed_phases: increment by 1
- Next: Phase 19 Project Workspace
</output>
