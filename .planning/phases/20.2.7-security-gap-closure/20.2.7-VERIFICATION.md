---
phase: 20.2.7-security-gap-closure
verified: 2026-03-25T00:00:00Z
status: passed
score: 9/9 must-haves verified
re_verification: false
gaps: []
human_verification: []
---

# Phase 20.2.7: Security Gap Closure — Verification Report

**Phase Goal:** Close all security gaps from the post-20.2.6 architectural review. Three concrete code fixes (GAP-5/6/7 Rust window guards, GAP-1 path confinement, GAP-3 WS first-message handshake) plus THREAT-MODEL.md corrections.
**Verified:** 2026-03-25T00:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                               | Status     | Evidence                                                                                                  |
|----|-----------------------------------------------------------------------------------------------------|------------|-----------------------------------------------------------------------------------------------------------|
| 1  | `reveal_path` in commands.rs requires main window (GAP-5)                                           | VERIFIED   | Line 171: `if require_main_window(&window).is_err()` before `is_absolute()` check at line 176            |
| 2  | `open_new_window` in commands.rs requires main window (GAP-6)                                       | VERIFIED   | Line 274: `require_main_window(&window)?` before `WebviewWindowBuilder::new` at line 276                 |
| 3  | `check_for_updates` in lib.rs requires main window (GAP-7)                                          | VERIFIED   | Line 144: `commands::require_main_window(&window)?` before `app.updater()` at line 145                   |
| 4  | `install_update` in lib.rs requires main window (GAP-7)                                             | VERIFIED   | Line 156: `commands::require_main_window(&window)?` before `app.updater()` at line 157                   |
| 5  | `/api/project/switch` confines paths to homedir() with normalize() and returns 400 for out-of-bounds | VERIFIED   | server.ts lines 340-348: `normalize(projectPath).startsWith(normalize(homedir()))` before `access()` check; returns 400 with "Path must be within home directory" |
| 6  | WS server authenticates via first-message `{type:"auth",token}`, closes with 4001 on invalid/timeout | VERIFIED   | ws-server.ts lines 108-196: connection starts UNAUTHENTICATED when launchToken set; 5-second timeout closes with 4001; invalid token closes with 4001 immediately |
| 7  | WS client sends `{type:"auth",token:launchToken}` as first message on open                         | VERIFIED   | useReconnectingWebSocket.ts lines 120-122: `ws.send(JSON.stringify({ type: "auth", token: launchToken }))` in `ws.onopen`; `launchToken` imported from `window-identity.ts` |
| 8  | THREAT-MODEL.md contains all required entries (localhost:4200, classify-intent-api.ts, GAP-2/GAP-9/DNS rebinding accepted-risk, Future Architecture) | VERIFIED | Lines 19, 25, 47-61, 82-95, 99-112, 116-140 confirmed present |
| 9  | Regression tests exist and pass: security-gap-closure.test.ts (14 tests) and security-ws-handshake.test.ts (6 tests) | VERIFIED | Both test files exist; `bun test` output: 14 pass / 0 fail; 6 pass / 0 fail                             |

**Score:** 9/9 truths verified

---

### Required Artifacts

| Artifact                                                                          | Expected                                          | Status     | Details                                                                                          |
|-----------------------------------------------------------------------------------|---------------------------------------------------|------------|--------------------------------------------------------------------------------------------------|
| `packages/mission-control/src-tauri/src/commands.rs`                             | GAP-5/6 window guards on reveal_path/open_new_window | VERIFIED | Both functions call `require_main_window` as first guard; `require_main_window` is `pub`         |
| `packages/mission-control/src-tauri/src/lib.rs`                                  | GAP-7 window guards on check_for_updates/install_update | VERIFIED | Both async fn accept `window: tauri::WebviewWindow` and call `commands::require_main_window(&window)?` before updater logic |
| `packages/mission-control/src/server.ts`                                         | GAP-1 homedir confinement on /api/project/switch  | VERIFIED   | `homedir` imported from `node:os`; `normalize` imported from `node:path`; confinement guard precedes `access()` call |
| `packages/mission-control/src/server/ws-server.ts`                               | GAP-3 first-message auth, no URL token            | VERIFIED   | Token no longer validated at upgrade; `authenticated: !launchToken` initial state; message handler gates all traffic until auth |
| `packages/mission-control/src/hooks/useReconnectingWebSocket.ts`                 | GAP-3 client sends auth as first message          | VERIFIED   | Imports `launchToken` from `window-identity`; sends `{type:"auth",token:launchToken}` in onopen  |
| `packages/mission-control/src/window-identity.ts`                                | Exports `launchToken`, sets it from startup-token | VERIFIED   | Line 23 `export let launchToken: string = ""`; `initWindowIdentity()` fetches `/api/auth/startup-token` and sets `launchToken` |
| `.planning/THREAT-MODEL.md`                                                       | All 8 required content items                      | VERIFIED   | See truth #8 — all sections confirmed present                                                    |
| `packages/mission-control/tests/security-gap-closure.test.ts`                    | 14 passing tests for GAP-1/5/6/7                  | VERIFIED   | File exists; `bun test` reports 14 pass / 0 fail in 368ms                                       |
| `packages/mission-control/tests/security-ws-handshake.test.ts`                   | 6 passing tests for GAP-3                         | VERIFIED   | File exists; `bun test` reports 6 pass / 0 fail in 6.41s (includes 5-second timeout test)       |

---

### Key Link Verification

| From                              | To                        | Via                                   | Status   | Details                                                                               |
|-----------------------------------|---------------------------|---------------------------------------|----------|---------------------------------------------------------------------------------------|
| `commands.rs:reveal_path`         | `require_main_window`     | direct call                           | WIRED    | Call at line 171 before any other logic                                               |
| `commands.rs:open_new_window`     | `require_main_window`     | direct call with `?` propagation      | WIRED    | Call at line 274 returns `Err(String)` to caller on non-main window                  |
| `lib.rs:check_for_updates`        | `commands::require_main_window` | direct call with `?` propagation | WIRED    | Call at line 144; `require_main_window` is `pub` in commands module                  |
| `lib.rs:install_update`           | `commands::require_main_window` | direct call with `?` propagation | WIRED    | Call at line 156                                                                      |
| `server.ts:/api/project/switch`   | `homedir()` + `normalize()` | inline guard before `access()`      | WIRED    | Guard at lines 340-348; imports at lines 1-2                                          |
| `ws-server.ts`                    | first-message auth gate   | `message()` handler + `open()` timeout | WIRED  | `authenticated: !launchToken` in upgrade data; timeout set in `open`; gate in `message` |
| `useReconnectingWebSocket.ts`     | `launchToken` from `window-identity` | ES module import + onopen send | WIRED | Import at line 13; send at lines 120-122 within `ws.onopen`                         |
| `window-identity.ts:initWindowIdentity` | `/api/auth/startup-token` | fetch + launchToken assignment | WIRED | Lines 66-78: fetches token and assigns to module-level `launchToken`                 |

---

### Data-Flow Trace (Level 4)

Not applicable. Phase 20.2.7 delivers security guards, configuration, and documentation — no new data-rendering components. The WS client sends `launchToken` which flows from the startup-token API (verified at the wiring level above).

---

### Behavioral Spot-Checks

| Behavior                                  | Command                                                                                   | Result              | Status  |
|-------------------------------------------|-------------------------------------------------------------------------------------------|---------------------|---------|
| GAP-1/5/6/7 tests pass                    | `bun test tests/security-gap-closure.test.ts`                                             | 14 pass / 0 fail    | PASS    |
| GAP-3 WS handshake tests pass (incl. 5s timeout) | `bun test tests/security-ws-handshake.test.ts`                                    | 6 pass / 0 fail     | PASS    |

---

### Requirements Coverage

No explicit requirement IDs were declared for this phase (phase requirement IDs: null). Coverage is tracked against the 9 must-have truths above, all of which are VERIFIED.

---

### Anti-Patterns Found

No blockers detected. The following observations are informational only:

| File                                           | Line | Pattern                                      | Severity | Impact                                                                                                  |
|------------------------------------------------|------|----------------------------------------------|----------|---------------------------------------------------------------------------------------------------------|
| `ws-server.ts`                                 | 108  | `authenticated: !launchToken`                | INFO     | When `launchToken` is falsy (tests/dev), connection auto-authenticates — documented intentional behavior |
| `useReconnectingWebSocket.ts`                  | 120  | `if (launchToken)` — skips send if token empty | INFO   | Intentional fallback for dev/test; `launchToken` is empty string by default, not null-checked          |

Neither pattern blocks goal achievement. Both are by-design for test backward compatibility and are explicitly documented in comments.

---

### Human Verification Required

None. All phase deliverables are programmatically verifiable (static code analysis + passing test suite).

---

### Gaps Summary

No gaps. All 9 must-haves are verified:

- **GAP-5 (reveal_path guard):** `require_main_window` called before path logic — confirmed.
- **GAP-6 (open_new_window guard):** `require_main_window` called before window build — confirmed.
- **GAP-7 (updater guards):** Both `check_for_updates` and `install_update` in lib.rs call `commands::require_main_window` as first statement — confirmed.
- **GAP-1 (path confinement):** `/api/project/switch` uses `normalize(homedir())` prefix check before `access()`, returns 400 on violation — confirmed.
- **GAP-3 server:** WS server no longer validates token on upgrade; connection starts UNAUTHENTICATED; 5-second timeout with 4001 close code; valid first-message auth transitions to AUTHENTICATED — confirmed.
- **GAP-3 client:** `useReconnectingWebSocket` sends `{type:"auth",token:launchToken}` in `ws.onopen`; `launchToken` is populated from `/api/auth/startup-token` via `initWindowIdentity()` — confirmed.
- **THREAT-MODEL.md:** All required entries present: `localhost:4200` in B37 section, `classify-intent-api.ts` in B57/B71 section, accepted-risk entries for GAP-2/GAP-9/DNS rebinding, `## Future Architecture` section — confirmed.
- **Regression tests:** Both test files exist and pass with zero failures — confirmed by direct `bun test` execution.

Phase 20.2.7 goal fully achieved.

---

_Verified: 2026-03-25T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
